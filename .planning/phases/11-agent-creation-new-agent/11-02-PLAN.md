---
phase: 11-agent-creation-new-agent
plan: "11-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/new-agent.md
  - workflows/team-studio.md
autonomous: true
skip_review: true

must_haves:
  truths:
    - "The /gsd:new-agent slash command exists and routes to the new-agent.md workflow at its installed path"
    - "Running /gsd:team and selecting 'Add agent' routes to /gsd:new-agent (not Phase 11 placeholder text)"
    - "The command file follows identical structure to commands/gsd/new-reviewer.md"
  artifacts:
    - path: "commands/gsd/new-agent.md"
      provides: "Slash command entry point for /gsd:new-agent"
      contains: "gsd:new-agent"
    - path: "commands/gsd/new-agent.md"
      provides: "execution_context referencing installed workflow path"
      contains: "get-shit-done-review-team/workflows/new-agent.md"
    - path: "workflows/team-studio.md"
      provides: "Live add_agent routing to /gsd:new-agent"
      contains: "/gsd:new-agent"
  key_links:
    - from: "commands/gsd/new-agent.md"
      to: "~/.claude/get-shit-done-review-team/workflows/new-agent.md"
      via: "execution_context @-reference"
      pattern: "execution_context"
    - from: "workflows/team-studio.md add_agent step"
      to: "/gsd:new-agent"
      via: "routing instruction in step body"
      pattern: "gsd:new-agent"
---

<objective>
Wire `/gsd:new-agent` into the GSD command system and update `/gsd:team` to route to it.

Two tasks:
1. Create `commands/gsd/new-agent.md` — the thin slash command entry point (identical structure to `commands/gsd/new-reviewer.md`)
2. Update the `add_agent` step in `workflows/team-studio.md` — replace the Phase 11 placeholder with live routing to `/gsd:new-agent`

Purpose: Without the command file, `/gsd:new-agent` does not exist as a Claude Code slash command. Without the team-studio update, `/gsd:team` → "Add agent" still shows a placeholder rather than directing users to the live command.

Output: `commands/gsd/new-agent.md` (new file), `workflows/team-studio.md` (updated add_agent step).
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/gsd/new-reviewer.md
@workflows/team-studio.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/gsd/new-agent.md — slash command entry point</name>
  <files>commands/gsd/new-agent.md</files>
  <action>
Create `D:/GSDteams/commands/gsd/new-agent.md` using the Write tool. This file is the slash command entry point for `/gsd:new-agent`. It must follow the identical structure as `commands/gsd/new-reviewer.md` — YAML frontmatter with name/description/allowed-tools, then `&lt;objective&gt;`, `&lt;execution_context&gt;`, and `&lt;process&gt;`.

The `execution_context` must reference the INSTALLED path `@~/.claude/get-shit-done-review-team/workflows/new-agent.md` — not the source path `D:/GSDteams/workflows/new-agent.md`. This is because Claude Code resolves @-references at command invocation time using the installed path (where install.sh copies the files).

Write the file with this exact content:

```
---
name: gsd:new-agent
description: Create a new agent role through a guided conversation
allowed-tools:
  - Read
  - Write
  - Bash
  - AskUserQuestion
---

<objective>
Create a new agent role in `.planning/TEAM.md` through a guided conversation.

Walks through: purpose → mode → trigger → output type → advisory details (advisory only)
→ scope (autonomous only) → confirmation gate → write.

**Creates or updates:**
- `.planning/TEAM.md` (always — role block appended, frontmatter roles: list updated)
- `agents/gsd-agent-{slug}.md` (autonomous agents only — agent prompt template)
</objective>

<execution_context>
@~/.claude/get-shit-done-review-team/workflows/new-agent.md
</execution_context>

<process>
Follow the new-agent workflow end-to-end.
Preserve all gates including the decision gate before writing.
Do not write to .planning/TEAM.md or agents/ until the user confirms in the decision_gate step.
If the user selects "Cancel" at the decision gate, exit immediately with no file writes.
</process>
```

Note: `install.sh` Section 5 already glob-copies `commands/gsd/*.md` — adding `new-agent.md` to this directory is sufficient. No install.sh change is needed.
  </action>
  <verify>
Read `D:/GSDteams/commands/gsd/new-agent.md` and verify:
1. File exists
2. YAML frontmatter contains `name: gsd:new-agent`
3. `allowed-tools` includes Read, Write, Bash, AskUserQuestion
4. `&lt;execution_context&gt;` contains `@~/.claude/get-shit-done-review-team/workflows/new-agent.md`
5. `&lt;process&gt;` instructs to follow the workflow end-to-end and preserve the decision gate
6. No reference to source path `D:/GSDteams/` anywhere in the file
  </verify>
  <done>
`commands/gsd/new-agent.md` exists with the correct YAML frontmatter (name: gsd:new-agent), the installed workflow @-reference in execution_context, and the process instruction to preserve all gates. The file will be auto-copied by `install.sh` Section 5 glob — no additional wiring needed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update workflows/team-studio.md add_agent step — live routing to /gsd:new-agent</name>
  <files>workflows/team-studio.md</files>
  <action>
Read `D:/GSDteams/workflows/team-studio.md` with the Read tool. Locate the `&lt;step name="add_agent"&gt;` section. It currently contains this placeholder text:

```
"To add an agent:
- For a **review-team role** (advisory, post-plan reviewer): run `/gsd:new-reviewer`
- For a full **agent role** (any mode, trigger, or output type): `/gsd:new-agent` is available
  in a future update (Phase 11)

Run `/gsd:new-reviewer` now to create a reviewer role."
```

Replace the entire `add_agent` step body with this updated content that routes to the now-live `/gsd:new-agent`:

```
&lt;step name="add_agent"&gt;
Route the user to the correct agent creation command based on what they want to create.

Inform the user:

"To add an agent:
- For a **full agent role** (any mode, trigger, or output type): run `/gsd:new-agent`
- For a **review-team role only** (advisory, post-plan reviewer, shortcut flow): run `/gsd:new-reviewer`

`/gsd:new-agent` supports all modes (advisory and autonomous), all triggers (pre-plan, post-plan,
post-phase, on-demand), and all output types (findings, notes, artifact). Use it for any new agent.

`/gsd:new-reviewer` is a shortcut for post-plan advisory reviewers only.

Run `/gsd:new-agent` to create your agent."

Return to present_actions.
&lt;/step&gt;
```

Write the full updated `team-studio.md` back using the Write tool (Read-then-Write — do not overwrite the entire file, preserve all other steps unchanged).

The replacement must be surgical: only the `add_agent` step body changes. All other steps (show_roster, present_actions, toggle_enabled, remove_agent, invoke_on_demand, view_history) remain exactly as they are.
  </action>
  <verify>
Read `D:/GSDteams/workflows/team-studio.md` and verify:
1. The `add_agent` step no longer contains the phrase "in a future update (Phase 11)"
2. The `add_agent` step contains `/gsd:new-agent` as the primary route
3. The `add_agent` step still mentions `/gsd:new-reviewer` as a shortcut for post-plan advisory roles
4. All other steps (show_roster, present_actions, toggle_enabled, remove_agent, invoke_on_demand, view_history) are unchanged
5. The file still has the correct `&lt;purpose&gt;` header at the top
  </verify>
  <done>
`workflows/team-studio.md` add_agent step routes to `/gsd:new-agent` as the primary agent creation command. The Phase 11 placeholder text ("available in a future update") is gone. All other steps in the file are unchanged.
  </done>
</task>

</tasks>

<verification>
After completing both tasks:
1. `D:/GSDteams/commands/gsd/new-agent.md` exists with `name: gsd:new-agent` in frontmatter
2. `D:/GSDteams/commands/gsd/new-agent.md` execution_context references the installed path (not source path)
3. `D:/GSDteams/workflows/team-studio.md` add_agent step mentions `/gsd:new-agent` as primary route
4. `D:/GSDteams/workflows/team-studio.md` add_agent step does NOT contain "Phase 11" placeholder text
5. All other team-studio.md steps are intact (check line count before vs after — should be similar with only add_agent section changing)
</verification>

<success_criteria>
- `/gsd:new-agent` exists as a runnable Claude Code slash command (via commands/gsd/new-agent.md)
- Running `/gsd:team` → "Add agent" shows `/gsd:new-agent` as the primary creation path, not a future-update placeholder
- install.sh does not need modification — Section 5 glob copies commands/gsd/*.md automatically
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-creation-new-agent/11-02-SUMMARY.md`
</output>

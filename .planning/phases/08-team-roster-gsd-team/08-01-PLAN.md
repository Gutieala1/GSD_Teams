---
phase: 08-team-roster-gsd-team
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - D:/GSDteams/workflows/agent-dispatcher.md
  - D:/GSDteams/templates/TEAM.md
  - D:/GSDteams/.planning/research/ARCHITECTURE.md
autonomous: true

must_haves:
  truths:
    - "Dispatcher skips any role where `enabled: false` is set — disabled role never reaches filter_by_trigger"
    - "Roles without an `enabled` field (all existing v1/v2 roles) are treated as enabled (default: true)"
    - "templates/TEAM.md Doc Writer example shows `enabled: false` as a valid per-role field"
    - "normalizeRole spec in ARCHITECTURE.md documents `enabled: true` default for ALL roles"
  artifacts:
    - path: "D:/GSDteams/workflows/agent-dispatcher.md"
      provides: "Updated dispatcher with enabled filter in read_and_parse_team_md step"
      contains: "active_roles"
    - path: "D:/GSDteams/templates/TEAM.md"
      provides: "Doc Writer example with enabled field"
      contains: "enabled:"
    - path: "D:/GSDteams/.planning/research/ARCHITECTURE.md"
      provides: "Updated normalizeRole spec with enabled default"
      contains: "role.enabled"
  key_links:
    - from: "agent-dispatcher.md read_and_parse_team_md step"
      to: "filter_by_trigger step"
      via: "active_roles list (enabled == true only)"
      pattern: "active_roles"
    - from: "normalizeRole"
      to: "enabled field"
      via: "?? true default injected for ALL roles regardless of version"
      pattern: "role.enabled.*true"
---

<objective>
Patch the agent dispatcher to filter out disabled agents before routing, and document the
`enabled` field in the templates and architecture spec.

Purpose: ROST-03 requires that disabled agents (`enabled: false`) never be invoked by the
dispatcher. The dispatcher (Phase 7) has no `enabled` check. This plan adds the filter.
The `enabled` field is also new — it must be added to templates/TEAM.md as a documented
example, and the normalizeRole spec in ARCHITECTURE.md must declare its default.

Output:
- agent-dispatcher.md updated: `enabled` default injected in normalizeRole block, `active_roles`
  filter produced before filter_by_trigger, filter_by_trigger receives active_roles not normalized_roles
- templates/TEAM.md updated: Doc Writer example gains `enabled: false` field with comment
- ARCHITECTURE.md updated: normalizeRole algorithm gains `role.enabled = role.enabled ?? true`
  line for ALL roles (not version-gated — enabled is a management field, not a schema version field)
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@D:/GSDteams/workflows/agent-dispatcher.md
@D:/GSDteams/templates/TEAM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch agent-dispatcher.md with enabled filter + update ARCHITECTURE.md normalizeRole spec</name>
  <files>
    D:/GSDteams/workflows/agent-dispatcher.md
    D:/GSDteams/.planning/research/ARCHITECTURE.md
  </files>
  <action>
**Part A: agent-dispatcher.md**

Read the current file at `D:/GSDteams/workflows/agent-dispatcher.md`.

In the `read_and_parse_team_md` step, locate the **end of the `normalizeRole` pseudocode block**
(after the closing `return role` line and the closing triple-backtick). Immediately after the
existing normalizeRole block, add a new section titled **"4. Apply enabled default and filter
active roles"**. This section must come BEFORE the content of the `filter_by_trigger` step.

Add this content to the `read_and_parse_team_md` step, after the existing normalizeRole block:

```
**4. Apply enabled default and filter active roles**

After applying normalizeRole to all roles, inject the `enabled` default and produce the
`active_roles` list:

```
For each role in normalized_roles:
  role.enabled = role.enabled ?? true
  # ?? means: use existing value if present and non-null, else use the default
  # This ensures v1 roles (no enabled field) and new v2 roles (enabled field absent) are
  # treated as enabled. Only an explicit enabled: false disables a role.

active_roles = [role for role in normalized_roles if role.enabled == true]
```

Pass `active_roles` (not `normalized_roles`) to the filter_by_trigger step.

If `active_roles` is empty (all roles disabled):
  Log: `Agent Dispatcher: all roles disabled — no-op`
  Return immediately. Do NOT proceed to filter_by_trigger.
```

Then, in the `filter_by_trigger` step, change the filter expression from:
```
matched_roles = [role for role in normalized_roles if role.trigger == trigger_type]
```
to:
```
matched_roles = [role for role in active_roles if role.trigger == trigger_type]
```

Also update the description at the top of the `filter_by_trigger` step to say:
"Filter active_roles to only those relevant to the current trigger context, then group by mode."
(replacing "normalized_roles" with "active_roles" in the first sentence)

**Part B: ARCHITECTURE.md normalizeRole algorithm**

Read `D:/GSDteams/.planning/research/ARCHITECTURE.md`.

Find the `normalizeRole Algorithm` section (around line 823). There are two locations with
normalizeRole pseudocode — update BOTH (the one around line 615 and the one around line 829).

For each normalizeRole pseudocode block, add `role.enabled = role.enabled ?? true` as the
LAST line inside the function body, OUTSIDE the `if version < 2:` block (i.e., at the same
indentation level as `if version < 2:`, after its closing line). This ensures enabled default
is injected for ALL roles regardless of version.

The updated pseudocode should look like:
```
function normalizeRole(role, version):
  if version < 2:
    role.mode             = role.mode             ?? "advisory"
    role.trigger          = role.trigger          ?? "post-plan"
    role.tools            = role.tools            ?? []
    role.output_type      = role.output_type      ?? "findings"
    role.commit           = role.commit           ?? false
    role.commit_message   = role.commit_message   ?? null
    role.scope            = role.scope            ?? {}
    role.scope.allowed_paths  = role.scope.allowed_paths  ?? []
    role.scope.allowed_tools  = role.scope.allowed_tools  ?? []
  role.enabled = role.enabled ?? true    # management field — default true for ALL roles
  return role
```

Write the updated ARCHITECTURE.md back. Preserve all other content unchanged.
  </action>
  <verify>
1. Read `D:/GSDteams/workflows/agent-dispatcher.md` — confirm the `read_and_parse_team_md` step
   contains the text `active_roles = [role for role in normalized_roles if role.enabled == true]`
2. Confirm the `filter_by_trigger` step uses `active_roles` (not `normalized_roles`) in its filter
3. Read `D:/GSDteams/.planning/research/ARCHITECTURE.md` — confirm `role.enabled = role.enabled ?? true`
   appears outside the `if version < 2:` block in the normalizeRole algorithm (at least in one location)
  </verify>
  <done>
- agent-dispatcher.md `read_and_parse_team_md` step contains `active_roles` filter after normalizeRole block
- `filter_by_trigger` step filters `active_roles` not `normalized_roles`
- ARCHITECTURE.md normalizeRole pseudocode has `role.enabled = role.enabled ?? true` outside the `if version < 2` block
- A role with `enabled: false` would be excluded from `active_roles` and never reach trigger matching
  </done>
</task>

<task type="auto">
  <name>Task 2: Add enabled field to templates/TEAM.md Doc Writer example</name>
  <files>
    D:/GSDteams/templates/TEAM.md
  </files>
  <action>
Read `D:/GSDteams/templates/TEAM.md`.

In the **Doc Writer** role's YAML config block (the triple-backtick yaml block under
`## Role: Doc Writer`), add the `enabled` field. It should appear immediately after the
`commit_message:` line (which ends with `"docs(auto): update docs after phase {phase}"`).

Add this line with a descriptive comment:
```
enabled: false           # true | false  (default: true when absent — omit to keep enabled)
```

The final Doc Writer YAML block should have the fields in this order:
1. name
2. focus
3. (blank line + comment)
4. mode
5. trigger
6. output_type
7. commit
8. commit_message
9. enabled            ← NEW
10. (blank line)
11. scope: block

Write the updated file back. Preserve all other content — the three v1 starter roles
(Security Auditor, Rules Lawyer, Performance Analyst) and the HTML comment before Doc Writer
remain unchanged.
  </action>
  <verify>
Read `D:/GSDteams/templates/TEAM.md` and confirm:
1. The Doc Writer YAML block contains the line `enabled: false`
2. The three v1 starter roles (security-auditor, rules-lawyer, performance-analyst) are unchanged
3. No other sections were modified
  </verify>
  <done>
- `templates/TEAM.md` Doc Writer YAML block contains `enabled: false` with a descriptive comment
- All existing v1 starter role blocks are untouched
- The `enabled` field position makes it easy to copy-paste as a reference when creating new roles
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Grep `D:/GSDteams/workflows/agent-dispatcher.md` for "active_roles" — must appear in both
   `read_and_parse_team_md` (definition) and `filter_by_trigger` (usage)
2. Grep `D:/GSDteams/workflows/agent-dispatcher.md` for "role.enabled" — must appear in the
   enabled default injection block
3. Grep `D:/GSDteams/.planning/research/ARCHITECTURE.md` for "role.enabled" — must appear
   outside the `if version < 2:` block
4. Grep `D:/GSDteams/templates/TEAM.md` for "enabled:" — must appear in Doc Writer YAML block
</verification>

<success_criteria>
- Dispatcher `read_and_parse_team_md` step: injects `enabled: true` default for all roles, produces `active_roles` list excluding disabled roles
- Dispatcher `filter_by_trigger` step: filters `active_roles` (not `normalized_roles`)
- A role with `enabled: false` in TEAM.md is excluded from dispatch — ROST-03 satisfied
- `templates/TEAM.md` shows `enabled: false` as a documented per-role field
- `ARCHITECTURE.md` normalizeRole spec includes `enabled: true` default outside the version gate
</success_criteria>

<output>
After completion, create `.planning/phases/08-team-roster-gsd-team/08-01-SUMMARY.md`
following the summary template.
</output>

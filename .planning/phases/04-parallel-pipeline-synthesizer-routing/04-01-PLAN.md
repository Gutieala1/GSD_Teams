---
phase: 04-parallel-pipeline-synthesizer-routing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/review-team.md
autonomous: true

must_haves:
  truths:
    - "All reviewer roles in TEAM.md are spawned in a single orchestrator message — not one at a time"
    - "spawn_reviewers reads ALL role definitions from a single TEAM.md read before issuing any Task() call"
    - "Each reviewer Task() receives only: ARTIFACT_PATH + its own role_definition text"
    - "After all reviewer Tasks complete, combined_findings JSON is built as a flat array tagged by reviewer"
    - "If all reviewers return 0 findings, the step writes an empty plan section and skips synthesis"
    - "return_status is updated to reflect Phase 4 PIPELINE COMPLETE format"
  artifacts:
    - path: "workflows/review-team.md"
      provides: "Parallel reviewer spawning + combined findings collection"
      contains: "Issue ALL of the following Task() calls in a SINGLE MESSAGE"
  key_links:
    - from: "spawn_reviewers step"
      to: "gsd-reviewer agent (all roles)"
      via: "N parallel Task() calls in one orchestrator message"
      pattern: "single message.*all.*Task"
    - from: "spawn_reviewers step"
      to: "synthesize step"
      via: "combined_findings JSON passed between steps"
      pattern: "combined_findings"
---

<objective>
Replace the Phase 3 single-reviewer spawn_reviewers step with a true parallel spawn of ALL roles
from TEAM.md in a single orchestrator message. Collect and merge findings from all reviewers into
a combined_findings JSON structure for the synthesizer.

Purpose: PIPE-01 requires all reviewer agents to run in a single workflow turn. The Phase 3
single-reviewer pattern must be replaced. This plan does the replacement — the synthesize step
remains a placeholder (04-03 implements it).

Output: review-team.md with an updated spawn_reviewers step and a Phase 4 return_status.
</objective>

<execution_context>
@D:/GSDteams/.planning/phases/04-parallel-pipeline-synthesizer-routing/04-RESEARCH.md
</execution_context>

<context>
@D:/GSDteams/.planning/PROJECT.md
@D:/GSDteams/.planning/ROADMAP.md
@D:/GSDteams/.planning/STATE.md
@D:/GSDteams/workflows/review-team.md
@D:/GSDteams/schemas/finding-schema.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace spawn_reviewers with parallel Task() spawning for all TEAM.md roles</name>
  <files>workflows/review-team.md</files>
  <action>
Replace the entire `<step name="spawn_reviewers">` block in `workflows/review-team.md`.

The current Phase 3 step selects only `roles_list[0]` (single reviewer). Replace it with the
Phase 4 parallel spawn pattern. The new step must:

**Part A — Extract All Role Definitions:**
Read `.planning/TEAM.md` once using the Read tool. For each role_name in `roles_list`
(populated by validate_team):
- Find the `## Role: {role_name}` header (case-insensitive match on the name from the YAML name: field)
- Extract all text from that header to the NEXT `## Role:` header (exclusive), or to EOF
- Store as `role_definitions[role_name]`

This single TEAM.md read populates all role definitions before any spawning begins. Per Pitfall 7
in the research: if the re-read TEAM.md produces a different roles list, log a WARNING and use the
re-read list as authoritative.

**Part B — Parallel Spawn (critical instruction):**
Include this explicit instruction:

"Issue ALL of the following Task() calls in a SINGLE MESSAGE — do not await between them.
All reviewers run simultaneously. Execution does not advance to collection until ALL complete."

Then write out the Task() template pattern. For each role_name in roles_list, one Task() block:

```
Task(
  subagent_type="gsd-reviewer",
  prompt="
    <objective>
    Review the sanitized artifact against your assigned role criteria.
    Return structured findings as JSON.
    </objective>

    <inputs>
    artifact_path: ${ARTIFACT_PATH}
    phase: ${PHASE_ID}
    plan: ${PLAN_NUM}
    </inputs>

    <role_definition>
    ${role_definitions[role_name]}
    </role_definition>

    <execution_context>
    @~/.claude/get-shit-done-review-team/agents/gsd-reviewer.md
    </execution_context>
  "
)
```

Include a note: "In practice this means writing out one Task() block per role in a single message.
For 3 starter roles: 3 Task() blocks issued together."

**Part C — Collect and Merge Findings:**
After ALL reviewer Tasks complete:

For each reviewer return:
1. Parse JSON to extract `findings` array
2. Log: `Review Team: {role_name}: {N} finding(s)`
3. If findings array is empty: log `Review Team: {role_name}: 0 findings (clean domain)` — not an error
4. Append all findings to combined_findings array (each finding already has `reviewer` field)

Build the combined_findings structure:
```json
{
  "phase": "${PHASE_ID}",
  "plan": "${PLAN_NUM}",
  "reviewer_count": N,
  "all_findings": [ ... merged flat array ... ],
  "per_reviewer": {
    "{role_name}": { "finding_count": N }
  }
}
```

**Part D — Early exit for zero findings:**
If combined all_findings is empty (ALL reviewers returned 0):
- Log: `Review Team: all reviewers — 0 findings, routing to log_and_continue`
- Write empty plan section to REVIEW-REPORT.md (path: `.planning/phases/${PHASE_ID}/REVIEW-REPORT.md`).
  Section format: `## Plan ${PLAN_NUM} — {ISO timestamp}\n\n**Reviewers:** {comma-separated}\n**Findings:** 0\n**Action:** log_and_continue\n\nNo findings — all reviewers returned clean.\n\n---`
  Check if file exists before writing: if not, create with `# Review Report — Phase ${PHASE_ID}` header first.
- Proceed directly to return_status with `routing: log_and_continue, findings: 0`
- SKIP the synthesize step — no synthesis needed

If combined all_findings is non-empty:
- Proceed to synthesize step with combined_findings JSON

Do NOT modify: `validate_team` step, `sanitize` step, `synthesize` step placeholder.
  </action>
  <verify>
Read the updated `workflows/review-team.md`. Confirm:
- spawn_reviewers step contains "Issue ALL of the following Task() calls in a SINGLE MESSAGE"
- The step loops over `roles_list` (not just `roles_list[0]`)
- combined_findings structure with `all_findings` flat array is present
- Early-exit for zero findings with REVIEW-REPORT.md write is documented
- validate_team, sanitize, and synthesize steps are unchanged
  </verify>
  <done>
spawn_reviewers step issues parallel Task() calls for ALL TEAM.md roles in a single message.
Combined findings collection merges all reviewer returns into a flat array tagged by reviewer.
Zero-findings early exit writes to REVIEW-REPORT.md and skips synthesis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update return_status to Phase 4 PIPELINE COMPLETE format</name>
  <files>workflows/review-team.md</files>
  <action>
Replace the `<step name="return_status">` block in `workflows/review-team.md`.

The current Phase 3 format says "REVIEWER COMPLETE" and includes "[Phase 4 not yet implemented]"
markers. Replace with the Phase 4 final format:

```markdown
## REVIEW PIPELINE: PIPELINE COMPLETE

**Phase:** ${PHASE_ID}
**Plan:** ${PLAN_NUM}
**Reviewers fired:** {N} ({comma-separated role names})
**Artifact:** ${ARTIFACT_PATH}
**Findings (pre-synthesis):** {total finding count across all reviewers}
**Final routing:** {FINAL_ROUTING}
**REVIEW-REPORT.md:** .planning/phases/${PHASE_ID}/REVIEW-REPORT.md

{If FINAL_ROUTING is log_and_continue:}
{N} finding(s) logged. Execution continues.

{If FINAL_ROUTING is block_and_escalate:}
Execution halted — user decision required. See findings above.

{If FINAL_ROUTING is send_for_rework:}
Execution halted — rework required. Address findings and re-execute this plan.

{If FINAL_ROUTING is send_to_debugger:}
Execution halted — debugger recommended. Run /gsd:debug with finding context.
```

Note: For `block_and_escalate`, return_status is NOT called until the user has responded
in the synthesize step's AskUserQuestion gate. The return_status step documents this:
"This step is reached only after the user has made a decision in block_and_escalate, or
after routing is log_and_continue. For send_for_rework and send_to_debugger, execution halts
in the synthesize step before reaching return_status."

Remove all "[Phase 4 not yet implemented]" language from return_status.
  </action>
  <verify>
Read `workflows/review-team.md`. Confirm:
- return_status uses "PIPELINE COMPLETE" (not "REVIEWER COMPLETE")
- return_status includes FINAL_ROUTING, reviewer count, REVIEW-REPORT.md path
- No "[Phase 4 not yet implemented]" text remains in return_status
- Note about block_and_escalate / send_for_rework / send_to_debugger halt is present
  </verify>
  <done>
return_status reflects the Phase 4 complete pipeline. "REVIEWER COMPLETE" is gone. PIPELINE COMPLETE
format includes all routing outcomes. Phase 3 placeholder language removed from this step.
  </done>
</task>

</tasks>

<verification>
Read workflows/review-team.md and verify the full file:
1. spawn_reviewers step: parallel Task() pattern with explicit "single message" instruction
2. spawn_reviewers step: combined_findings JSON structure with flat all_findings array
3. spawn_reviewers step: zero-findings early exit with REVIEW-REPORT.md write
4. synthesize step: still contains "[Phase 4 -- not yet implemented]" (04-03 implements it)
5. return_status: PIPELINE COMPLETE format with routing-conditional messaging
6. No regressions in validate_team or sanitize steps
</verification>

<success_criteria>
- spawn_reviewers explicitly states "Issue ALL Task() calls in a SINGLE MESSAGE"
- All roles from roles_list are spawned (not just roles_list[0])
- combined_findings merges all reviewer findings into one flat array
- Zero-findings path writes REVIEW-REPORT.md and skips synthesize
- return_status uses PIPELINE COMPLETE format
- synthesize placeholder intact for 04-03
</success_criteria>

<output>
After completion, create `.planning/phases/04-parallel-pipeline-synthesizer-routing/04-01-SUMMARY.md`
</output>

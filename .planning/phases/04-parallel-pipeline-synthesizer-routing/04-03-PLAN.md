---
phase: 04-parallel-pipeline-synthesizer-routing
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - workflows/review-team.md
autonomous: false

must_haves:
  truths:
    - "synthesize step spawns gsd-review-synthesizer via Task() with combined_findings injected as <combined_findings> tags"
    - "After synthesizer returns, workflow validates source_finding_ids for every unique_finding before acting on routing"
    - "Workflow checks for critical severity BEFORE reading overall_routing — critical always forces block_and_escalate"
    - "block_and_escalate halts with AskUserQuestion — execution does not proceed until user responds"
    - "send_for_rework halts and presents findings — user must address findings and re-execute the plan"
    - "send_to_debugger halts and presents findings — user instructed to run /gsd:debug"
    - "log_and_continue writes to REVIEW-REPORT.md and proceeds without halting"
    - "REVIEW-REPORT.md is written on ALL routing paths (including block_and_escalate) before acting on route"
    - "REVIEW-REPORT.md append logic: Read-then-Write if file exists, create-with-header if not"
    - "return_status is reached only after log_and_continue or after user responds to block_and_escalate"
  artifacts:
    - path: "workflows/review-team.md"
      provides: "Full synthesize step + all 4 routing actions + REVIEW-REPORT.md writer"
      contains: "FINAL_ROUTING = block_and_escalate"
  key_links:
    - from: "synthesize step"
      to: "gsd-review-synthesizer agent"
      via: "Task() with <combined_findings> injection"
      pattern: "subagent_type.*gsd-review-synthesizer"
    - from: "workflow routing enforcement"
      to: "synthesizer overall_routing"
      via: "critical severity check BEFORE reading overall_routing"
      pattern: "any.*severity.*critical.*block_and_escalate"
    - from: "synthesize step"
      to: "REVIEW-REPORT.md"
      via: "Read-then-Write append on all routing paths"
      pattern: "REVIEW-REPORT\\.md"
---

<objective>
Implement the synthesize step in `workflows/review-team.md` — replacing the Phase 4 placeholder.
The step spawns gsd-review-synthesizer, validates traceability, enforces deterministic routing
(critical severity always overrides to block_and_escalate regardless of synthesizer suggestion),
writes REVIEW-REPORT.md on all paths, and acts on the final routing decision.

Purpose: SYNTH-01 through SYNTH-05 require the full synthesis pipeline. This plan completes it.
Depends on 04-01 (combined_findings built in spawn_reviewers) and 04-02 (synthesizer agent exists).

Output: review-team.md with a complete synthesize step — all 4 routing destinations reachable.
</objective>

<execution_context>
@D:/GSDteams/.planning/phases/04-parallel-pipeline-synthesizer-routing/04-RESEARCH.md
@D:/GSDteams/.planning/phases/04-parallel-pipeline-synthesizer-routing/04-01-SUMMARY.md
@D:/GSDteams/.planning/phases/04-parallel-pipeline-synthesizer-routing/04-02-SUMMARY.md
</execution_context>

<context>
@D:/GSDteams/.planning/PROJECT.md
@D:/GSDteams/.planning/STATE.md
@D:/GSDteams/workflows/review-team.md
@D:/GSDteams/agents/gsd-review-synthesizer.md
@D:/GSDteams/schemas/finding-schema.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the synthesize step — synthesizer spawn, validation, routing enforcement, REVIEW-REPORT.md write, routing actions</name>
  <files>workflows/review-team.md</files>
  <action>
Replace the `<step name="synthesize">` block in `workflows/review-team.md`.

The current content is the placeholder: "[Phase 4 -- not yet implemented]". Replace it with
the full implementation. The step has 5 parts:

---

**Part A — Spawn Synthesizer**

```
Task(
  subagent_type="gsd-review-synthesizer",
  prompt="
    <objective>
    Deduplicate cross-reviewer findings. Assign routing for each unique finding.
    Return structured synthesis JSON.
    </objective>

    <inputs>
    Phase: ${PHASE_ID}
    Plan: ${PLAN_NUM}
    </inputs>

    <combined_findings>
    ${combined_findings_json}
    </combined_findings>

    <execution_context>
    @~/.claude/get-shit-done-review-team/agents/gsd-review-synthesizer.md
    </execution_context>
  "
)
```

`combined_findings_json` = the combined findings JSON built at the end of the spawn_reviewers step.
The combined findings JSON is passed directly in the prompt — NOT written to a file first.

---

**Part B — Validate No-New-Findings (before acting on routing)**

After synthesizer returns:
1. Parse synthesizer JSON to extract `unique_findings` and `synthesis_errors`
2. Check `synthesis_errors` array — if non-empty, log each error as WARNING
3. Build input_ids set from `combined_findings.all_findings` (all reviewer finding IDs)
4. For each unique_finding in synthesizer output:
   a. If `source_finding_ids` is empty or null: log `Review Team: WARNING — finding {id} has no source_finding_ids (pipeline violation)`
   b. For each source_id in `source_finding_ids`: if source_id NOT in input_ids, log `Review Team: WARNING — finding {id} has invalid source_finding_id {source_id}`
5. If ANY validation failure detected: log `Review Team: WARNING — synthesizer validation failed, using reviewer findings directly (fallback)`
   Fallback: treat combined_findings.all_findings as-is (un-deduplicated), routing based on highest severity in the raw findings

---

**Part C — Apply Deterministic Routing Override (severity minimum)**

This check runs BEFORE reading `synthesizer.overall_routing`:

```
Step 1: HAS_CRITICAL = any unique_finding where severity == "critical"

Step 2:
  if HAS_CRITICAL:
    FINAL_ROUTING = "block_and_escalate"
    Log: "Review Team: critical finding detected — routing overridden to block_and_escalate (hardcoded minimum)"
  else:
    FINAL_ROUTING = synthesizer.overall_routing
    Log: "Review Team: routing → {FINAL_ROUTING}"

Step 3: If FINAL_ROUTING != synthesizer.overall_routing (and not the critical override case):
    Log: "Review Team: routing override — synthesizer suggested {synthesizer.overall_routing}, enforced {FINAL_ROUTING}"
```

This enforcement applies even if the synthesizer assigned `block_and_escalate` to a critical finding
in its own `final_routing` — the workflow does the check independently as a safety layer.

---

**Part D — Write REVIEW-REPORT.md (ALL routing paths, before acting on route)**

Write before acting on FINAL_ROUTING — the finding is logged even if execution halts.

Path: `.planning/phases/${PHASE_ID}/REVIEW-REPORT.md`

Append logic (Pitfall 4 prevention):
1. Attempt to Read `.planning/phases/${PHASE_ID}/REVIEW-REPORT.md`
2. If file does NOT exist: build content = `# Review Report — Phase ${PHASE_ID}\n\n---\n\n` + plan section
3. If file EXISTS: build content = existing_content + `\n\n` + plan section (append)
4. Write the full content back

Plan section format:
```markdown
## Plan ${PLAN_NUM} — {ISO timestamp, e.g. 2026-02-26T03:15:00Z}

**Reviewers:** {comma-separated from synthesizer.reviewer_coverage}
**Findings:** {len(unique_findings)} | **Deduped:** {synthesizer.dedup_count}
**Action:** {FINAL_ROUTING}

| ID | Reviewer | Severity | Description | Evidence | Routing |
|----|----------|----------|-------------|----------|---------|
{one row per unique_finding: | {id} | {reviewer} | {severity} | {description (truncated to 80 chars)} | {evidence (truncated to 60 chars)} | {final_routing} |}

---
```

If 0 unique_findings (fallback from validation failure used raw findings, or synthesizer returned empty):
```markdown
## Plan ${PLAN_NUM} — {ISO timestamp}

**Reviewers:** {comma-separated}
**Findings:** 0
**Action:** log_and_continue

No findings — all reviewers returned clean.

---
```

---

**Part E — Act on FINAL_ROUTING**

All four routing destinations:

**block_and_escalate:**
Present to user using AskUserQuestion (or equivalent halt):

```
## REVIEW TEAM: EXECUTION BLOCKED

**Phase:** ${PHASE_ID}
**Plan:** ${PLAN_NUM}

Critical finding(s) require your decision before execution continues.

### Critical Findings

| ID | Reviewer | Severity | Finding | Evidence |
|----|----------|----------|---------|----------|
{rows for unique_findings where severity == "critical"}

---

Findings logged to REVIEW-REPORT.md.

**Options:**
- "Continue" — acknowledge finding, continue with next plan
- "Fix first" — stop execution, address finding manually
- "Rework" — re-execute this plan with the finding as context

Execution will NOT continue until you respond.
```

Wait for user response. Do NOT call return_status until user decides.
Note: block_and_escalate overrides auto_advance mode — user input is always required.

**send_for_rework:**
Present findings and halt:

```
## REVIEW TEAM: REWORK REQUIRED

**Phase:** ${PHASE_ID}
**Plan:** ${PLAN_NUM}
**Findings requiring rework:** {count of high-severity findings}

{Table of high-severity findings}

Findings logged to REVIEW-REPORT.md.
Execution halted. Suggested action: address the finding(s) above, then re-execute this plan.
```

**send_to_debugger:**
Present findings and halt:

```
## REVIEW TEAM: DEBUGGER RECOMMENDED

**Phase:** ${PHASE_ID}
**Plan:** ${PLAN_NUM}

{Table of medium findings with send_to_debugger routing}

Findings logged to REVIEW-REPORT.md.
Suggested action: run /gsd:debug with the finding context above.
```

**log_and_continue:**
```
Log: "Review Team: {N} finding(s) logged to REVIEW-REPORT.md — continuing execution"
Proceed to return_status immediately. No halt. No user presentation.
```
  </action>
  <verify>
Read the updated `workflows/review-team.md` synthesize step. Confirm:
1. Synthesizer Task() spawned with `<combined_findings>` injection (not a file path)
2. source_finding_ids validation runs before routing decision
3. Critical severity check (`any unique_finding where severity == "critical"`) occurs BEFORE reading `synthesizer.overall_routing`
4. REVIEW-REPORT.md write uses Read-then-append pattern (not overwrite)
5. block_and_escalate uses AskUserQuestion / halt requiring user response
6. send_for_rework halts with guidance to re-execute the plan
7. send_to_debugger halts with /gsd:debug guidance
8. log_and_continue proceeds without halt
9. REVIEW-REPORT.md write occurs on ALL paths before acting on routing
  </verify>
  <done>
synthesize step is fully implemented. All 4 routing destinations are reachable.
Critical severity hardcoded to block_and_escalate in workflow code (not delegated to synthesizer).
REVIEW-REPORT.md appended on every routing path.
block_and_escalate uses AskUserQuestion gate — execution does not continue until user responds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete Phase 4 pipeline</name>
  <action>
Human verification of the complete Phase 4 pipeline implementation.
Read workflows/review-team.md and agents/gsd-review-synthesizer.md end to end.
Confirm all 15 verification items in the how-to-verify section below.
  </action>
  <verify>
Read workflows/review-team.md end to end:

1. spawn_reviewers step says "Issue ALL of the following Task() calls in a SINGLE MESSAGE"
2. spawn_reviewers loops over all roles_list (not just roles_list[0])
3. spawn_reviewers builds combined_findings with flat all_findings array
4. synthesize step spawns gsd-review-synthesizer with `<combined_findings>` injection
5. synthesize step validates source_finding_ids for every unique_finding BEFORE routing
6. synthesize step checks `any unique_finding.severity == "critical"` BEFORE reading `synthesizer.overall_routing`
7. block_and_escalate presents to user and waits (AskUserQuestion / halt)
8. REVIEW-REPORT.md is written using Read-then-append on ALL routing paths
9. log_and_continue proceeds without halt
10. return_status uses PIPELINE COMPLETE format

Read agents/gsd-review-synthesizer.md:
11. YAML frontmatter: name: gsd-review-synthesizer, tools: Read, Write, color: green
12. critical_rules rule 1: "DO NOT generate findings not present in your input"
13. source_finding_ids in output schema
14. synthesis_note field on root output (not as new findings)
15. Output is JSON only (no prose before or after)

Cross-check SYNTH-04 (no invented findings): Does gsd-review-synthesizer.md explicitly prohibit findings with empty source_finding_ids in both the role block AND critical_rules?

Cross-check PIPE-01 (single turn): Does spawn_reviewers explicitly say "SINGLE MESSAGE"?
  </verify>
  <done>User types "approved" — pipeline verified correct end to end.</done>
  <resume-signal>Type "approved" if the pipeline looks correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:
1. All 7 Phase 4 success criteria are satisfied in the workflow and agent files
2. PIPE-01: spawn_reviewers single-message parallelism instruction present
3. SYNTH-01: synthesizer receives all findings from all reviewers (combined_findings)
4. SYNTH-02: deduplication logic in gsd-review-synthesizer.md Steps 1-2
5. SYNTH-03: workflow code checks critical severity BEFORE reading overall_routing
6. SYNTH-04: source_finding_ids validation in workflow + no-new-findings guards in agent
7. SYNTH-05: REVIEW-REPORT.md at {phase_dir}/REVIEW-REPORT.md, appended per-plan
</verification>

<success_criteria>
- synthesize step fully replaces the "[Phase 4 -- not yet implemented]" placeholder
- All 4 routing destinations implemented and reachable in the synthesize step
- Critical severity hardcoded to block_and_escalate in workflow code (deterministic)
- REVIEW-REPORT.md append logic: Read-then-Write, per-plan section header
- block_and_escalate requires user decision before execution continues
- Checkpoint passes — human has verified the complete pipeline implementation
</success_criteria>

<output>
After completion, create `.planning/phases/04-parallel-pipeline-synthesizer-routing/04-03-SUMMARY.md`
</output>

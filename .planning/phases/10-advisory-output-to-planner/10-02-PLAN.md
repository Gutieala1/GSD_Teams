---
phase: 10-advisory-output-to-planner
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - D:/GSDteams/workflows/agent-dispatcher.md
  - D:/GSDteams/.planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "Dispatcher route_by_mode splits advisory_roles by output_type before routing"
    - "advisory_notes_roles (output_type: notes) spawn Task() per agent and return <agent_notes> block"
    - "advisory_findings_roles only route to review-team.md when trigger_type == 'post-plan'"
    - "Pre-plan + findings combination logs a warning and skips (no review-team.md call)"
    - "Output text from dispatcher includes <agent_notes> block when notes agents fired"
    - "No behavioral change for existing post-plan advisory findings path"
    - "ROADMAP Phase 10 success criterion 2 updated to reflect pre-plan findings agents are skipped for pre-plan triggers"
  artifacts:
    - path: "D:/GSDteams/workflows/agent-dispatcher.md"
      provides: "Updated dispatcher with output_type-split routing in route_by_mode step"
      contains: "advisory_notes_roles"
    - path: "D:/GSDteams/.planning/ROADMAP.md"
      provides: "Updated success criterion 2 reflecting pre-plan findings behavior"
      contains: "pre-plan triggers, findings agents are skipped"
  key_links:
    - from: "route_by_mode advisory path"
      to: "output_type split logic"
      via: "advisory_notes_roles + advisory_findings_roles split before routing"
      pattern: "advisory_notes_roles"
    - from: "advisory_notes_roles spawn loop"
      to: "<agent_notes> block return"
      via: "Task() per notes agent → collect markdown → wrap in <agent_notes> tags"
      pattern: "agent_notes"
    - from: "advisory_findings_roles path"
      to: "trigger_type guard"
      via: "only call review-team.md when trigger_type == post-plan"
      pattern: "post-plan"
---

<objective>
Update agent-dispatcher.md's route_by_mode step to split advisory roles by output_type:
- output_type: notes roles → spawn Task() per agent, collect structured markdown, return as `<agent_notes>` block
- output_type: findings roles → route to review-team.md only for post-plan trigger; skip with warning for pre-plan

Also update ROADMAP.md Phase 10 success criterion 2 to accurately reflect the implemented behavior: findings agents are skipped for pre-plan triggers (no SUMMARY.md exists at that point), and route through the synthesizer pipeline for post-plan triggers only.

Purpose: Without this change, the pre_plan_agent_gate at step 8 in plan-phase.md would receive no structured notes from the dispatcher — it would receive review pipeline output (or nothing). The dispatcher must know how to return notes directly for the planner injection pattern to work. The ROADMAP update removes the internal contradiction between the plan-list annotation and the success criterion so goal-backward verification passes after execution.

Output: Updated D:/GSDteams/workflows/agent-dispatcher.md + updated D:/GSDteams/.planning/ROADMAP.md
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/GSDteams/.planning/PROJECT.md
@D:/GSDteams/.planning/ROADMAP.md
@D:/GSDteams/.planning/STATE.md
@D:/GSDteams/workflows/agent-dispatcher.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update route_by_mode step with output_type split routing</name>
  <files>D:/GSDteams/workflows/agent-dispatcher.md</files>
  <action>
Read D:/GSDteams/workflows/agent-dispatcher.md. Locate the `<step name="route_by_mode">` block.
Replace the entire step body with the new output_type-split routing logic below.

**Current advisory path (to replace):**
The current step routes ALL advisory_roles to review-team.md unconditionally. Replace this
with a split based on output_type.

**New route_by_mode step body:**

```
<step name="route_by_mode">
Route matched roles to their execution paths by output_type. Notes agents return structured
markdown directly. Findings agents route to the review pipeline (post-plan only).
Autonomous roles are logged as stubs.

**Split advisory roles by output_type**

```
advisory_notes_roles    = [r for r in advisory_roles if r.output_type == "notes"]
advisory_findings_roles = [r for r in advisory_roles if r.output_type != "notes"]
```

**Handle advisory findings roles (output_type: findings or default)**

If `advisory_findings_roles` is non-empty:

  If `trigger_type` == "post-plan":
    Call the review pipeline (unchanged behavior):

    ```
    @~/.claude/get-shit-done-review-team/workflows/review-team.md
    ```

    Pass these inputs:
    - SUMMARY_PATH = summary_path input
    - PHASE_ID     = phase_id input
    - PLAN_NUM     = plan_num input

    IMPORTANT: Do NOT pass a filtered role list. review-team.md reads .planning/TEAM.md
    itself and performs its own role validation. The dispatcher does not pre-filter.

  If `trigger_type` != "post-plan" (e.g. "pre-plan"):
    Log: `Agent Dispatcher: {N} advisory findings agent(s) ignored for trigger '{trigger_type}' — output_type: findings requires post-plan trigger. Use output_type: notes for pre-plan agents.`
    Do NOT call review-team.md. Do NOT spawn any Task(). Continue to notes handling below.

**Handle advisory notes roles (output_type: notes)**

If `advisory_notes_roles` is non-empty:

  Initialize: `collected_notes = []`

  For each role in advisory_notes_roles (spawn ALL in a single message — true parallelism,
  same pattern as spawn_reviewers in review-team.md):

    Spawn a Task() with this prompt:
    ```
    You are {role.name}.

    {role definition block from TEAM.md — the full ## Role: {slug} section}

    Your task: Review the current phase context and return structured advisory notes.
    These notes will be passed to the GSD planner as input before planning begins.
    The planner may incorporate or explicitly disregard your notes — always provide notes.

    Phase context:
    - Phase ID: {phase_id}
    - Phase goal and requirements: read from .planning/ROADMAP.md and .planning/phases/{phase_id}/
    - Available context files: .planning/STATE.md, .planning/ROADMAP.md

    Return structured markdown with:
    ## Advisory Notes: {brief topic}

    **Key observation:** {finding relevant to the plan}

    **Recommendation:** {specific, actionable suggestion for the planner}

    **Confidence:** HIGH | MEDIUM | LOW

    ---

    Focus on your declared domain: {role.focus}
    Do not address areas outside your domain.
    ```

  Collect all Task() return values. For each: append the role name and the returned markdown
  to `collected_notes`.

  Build the `<agent_notes>` block:
  ```
  AGENT_NOTES_BLOCK = "<agent_notes>\n"
  AGENT_NOTES_BLOCK += "Advisory agents ran before this plan was created. These notes are INPUT — "
  AGENT_NOTES_BLOCK += "incorporate them where relevant, or explicitly disregard with a brief note. "
  AGENT_NOTES_BLOCK += "Always produce a plan.\n\n"
  for (role_name, notes_markdown) in collected_notes:
    AGENT_NOTES_BLOCK += f"## {role_name}\n\n{notes_markdown}\n\n"
  AGENT_NOTES_BLOCK += "</agent_notes>"
  ```

  Return `AGENT_NOTES_BLOCK` as part of the dispatcher output text. The calling gate step
  (pre_plan_agent_gate in plan-phase.md) extracts this block from the dispatcher's return
  value by looking for the `<agent_notes>` ... `</agent_notes>` span.

  Log: `Agent Dispatcher: {N} advisory notes agent(s) fired. <agent_notes> block returned.`

If `advisory_notes_roles` is empty: do not include any `<agent_notes>` block in output.
Log only if notes agents were expected but none matched (e.g. zero notes roles for pre-plan).

**Handle autonomous roles (stub — Phase 9)**

If `autonomous_roles` is non-empty:

  For each role in autonomous_roles:
    Log: `Agent Dispatcher: autonomous agent '{role.name}' — autonomous execution not yet implemented (Phase 9). Skipping.`

  Do NOT spawn any Task(). Do NOT crash. Do NOT call any pipeline.

**Return**

After handling all role groups, the dispatcher returns normally to the calling gate step.
For pre-plan calls with notes agents: the return text includes the `<agent_notes>` block.
For post-plan calls: flow returns to offer_next in execute-plan.md (unchanged).
</step>
```

**Key constraints:**
- The existing post-plan + findings path (calling review-team.md) must be UNCHANGED in behavior
- Do NOT filter roles before passing to review-team.md — it reads TEAM.md itself
- The `<agent_notes>` block is only present in the return value when notes agents actually fired
- The autonomous stub log message is preserved unchanged from Phase 9
- Update the `<purpose>` block at the top of agent-dispatcher.md to reflect Phase 10:
  Change "For advisory post-plan roles: delegates unchanged to the review-team.md pipeline"
  to: "For advisory roles: routes output_type:notes to direct return (<agent_notes> block);
  routes output_type:findings to review-team.md (post-plan only)"
  Also update "Calling point:" to mention both pre_plan_agent_gate (plan-phase.md) and
  review_team_gate (execute-plan.md).
  </action>
  <verify>
# Verify the split logic is present
grep -n "advisory_notes_roles" D:/GSDteams/workflows/agent-dispatcher.md
# Verify findings guard is present
grep -n "post-plan" D:/GSDteams/workflows/agent-dispatcher.md
# Verify agent_notes block construction is present
grep -n "agent_notes" D:/GSDteams/workflows/agent-dispatcher.md
# Verify autonomous stub preserved
grep -n "autonomous execution not yet implemented" D:/GSDteams/workflows/agent-dispatcher.md
  </verify>
  <done>
- agent-dispatcher.md route_by_mode step contains output_type split logic (advisory_notes_roles + advisory_findings_roles)
- advisory_findings_roles path is gated on trigger_type == "post-plan"
- advisory_notes_roles path spawns Task() per agent and returns <agent_notes> block
- Autonomous stub log message preserved unchanged
- Purpose block updated to reflect dual calling points
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP Phase 10 success criterion 2</name>
  <files>D:/GSDteams/.planning/ROADMAP.md</files>
  <action>
Read D:/GSDteams/.planning/ROADMAP.md. Locate the Phase 10 section under "## Phase Details".

Find the current success criterion 2 text:
```
  2. A pre-plan advisory agent with `output_type: findings` fires before plan creation and its output routes through the existing synthesizer pipeline — it does not appear as `<agent_notes>` and does not block plan creation
```

Replace it with:
```
  2. An advisory agent with `output_type: findings` routes through the existing synthesizer pipeline for post-plan triggers only. For pre-plan triggers, findings agents are skipped with a log message (no SUMMARY.md exists at pre-plan time). Findings agents do not appear as `<agent_notes>` and do not block plan or execution creation.
```

This resolves the internal contradiction between the plan-list annotation
("findings → review pipeline post-plan only") and the original success criterion
(which implied findings agents fire at pre-plan time). The new text accurately describes
the implemented behavior: findings agents are skipped at pre-plan because no SUMMARY.md
exists yet; they only route to the synthesizer pipeline for post-plan triggers where a
SUMMARY.md is available.

No other changes to ROADMAP.md. Preserve all formatting, checkboxes, and table rows exactly.
  </action>
  <verify>
grep -A 3 "output_type: findings" D:/GSDteams/.planning/ROADMAP.md | grep "post-plan triggers only"
  </verify>
  <done>
ROADMAP.md Phase 10 success criterion 2 contains the updated text: "routes through the existing synthesizer pipeline for post-plan triggers only. For pre-plan triggers, findings agents are skipped with a log message (no SUMMARY.md exists at pre-plan time)."
  </done>
</task>

</tasks>

<verification>
Run full verification:
1. `grep -n "advisory_notes_roles" D:/GSDteams/workflows/agent-dispatcher.md` — must match in route_by_mode step
2. `grep -n "advisory_findings_roles" D:/GSDteams/workflows/agent-dispatcher.md` — must match
3. `grep -c "post-plan" D:/GSDteams/workflows/agent-dispatcher.md` — must be >= 1 (trigger guard)
4. `grep -n "agent_notes" D:/GSDteams/workflows/agent-dispatcher.md` — must show notes block construction
5. `grep -n "autonomous execution not yet implemented" D:/GSDteams/workflows/agent-dispatcher.md` — must still be present (stub preserved)
6. `grep -n "review-team.md" D:/GSDteams/workflows/agent-dispatcher.md` — must show it is still called (post-plan path intact)
7. `grep -n "post-plan triggers only" D:/GSDteams/.planning/ROADMAP.md` — must match Phase 10 success criterion 2
</verification>

<success_criteria>
- route_by_mode splits advisory_roles into advisory_notes_roles + advisory_findings_roles
- advisory_findings_roles only calls review-team.md when trigger_type == "post-plan"; logs warning and skips for other triggers
- advisory_notes_roles spawns Task() per agent (single message for parallelism), collects notes, returns <agent_notes> block
- <agent_notes> block is only included in return text when notes agents actually fired
- Post-plan findings path behavior is unchanged — review-team.md still called for advisory post-plan roles
- Autonomous stub unchanged
- ROADMAP Phase 10 success criterion 2 accurately describes that findings agents are skipped for pre-plan triggers and route through synthesizer for post-plan triggers only
</success_criteria>

<output>
After completion, create `D:/GSDteams/.planning/phases/10-advisory-output-to-planner/10-02-SUMMARY.md`
</output>

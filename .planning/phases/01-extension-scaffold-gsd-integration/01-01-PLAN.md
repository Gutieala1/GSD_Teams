---
phase: 01-extension-scaffold-gsd-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/TEAM.md
  - agents/.gitkeep
  - workflows/.gitkeep
  - commands/gsd/.gitkeep
autonomous: true

must_haves:
  truths:
    - "Extension repo directory structure exists with correct folder layout matching PROJECT.md architecture"
    - "templates/TEAM.md exists with 3 fully visible example roles: Security Auditor, Rules Lawyer, Performance Analyst"
    - "Each role in TEAM.md has a YAML block with name + focus, a What this role reviews list, Severity thresholds, and Routing hints"
    - "Each role in TEAM.md has a visible callout marking it as an example to customize"
  artifacts:
    - path: "templates/TEAM.md"
      provides: "Starter team template with 3 example roles"
      contains: "## Role: Security Auditor"
    - path: "templates/TEAM.md"
      provides: "Machine-parseable role YAML blocks"
      contains: "name: security-auditor"
    - path: "agents/.gitkeep"
      provides: "Agents directory placeholder for future phases"
    - path: "workflows/.gitkeep"
      provides: "Workflows directory placeholder for future phases"
    - path: "commands/gsd/.gitkeep"
      provides: "Commands directory placeholder for future phases"
  key_links:
    - from: "templates/TEAM.md"
      to: "review-team.md workflow (Phase 2)"
      via: "## Role: section headers used as split anchors"
      pattern: "^## Role:"
    - from: "templates/TEAM.md"
      to: ".planning/TEAM.md in target project"
      via: "install.sh cp with existence guard"
      pattern: "[ -f .planning/TEAM.md ]"
---

<objective>
Create the extension repo scaffold and the TEAM.md starter template that ships with the extension.

Purpose: Phase 2 through 5 all build on this repo structure. The TEAM.md starter template teaches users the role format and satisfies TEAM-04. All placeholder directories for future-phase files must exist now so install.sh can copy them without extra logic.

Output: Extension directory structure with placeholder .gitkeep files + templates/TEAM.md with 3 fully-specified example roles.
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extension-scaffold-gsd-integration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extension repo directory scaffold</name>
  <files>
    agents/.gitkeep
    workflows/.gitkeep
    commands/gsd/.gitkeep
    scripts/.gitkeep
  </files>
  <action>
In the repo root (D:/GSDteams), create the following directory structure with empty .gitkeep placeholder files. All directories and files are relative to the repo root.

Directory structure to create:
```
agents/           ← Phase 2-4 agent files land here
workflows/        ← Phase 2, 5 workflow files land here
commands/gsd/     ← Phase 5 /gsd:new-reviewer command lands here
scripts/          ← Phase 1 Python patch scripts land here
templates/        ← TEAM.md starter template (created in Task 2)
```

Create .gitkeep files in each directory so git tracks them:
- `agents/.gitkeep`
- `workflows/.gitkeep`
- `commands/gsd/.gitkeep`
- `scripts/.gitkeep`

Do NOT create .gitkeep in `templates/` — that directory will have real content from Task 2.

Note: The `D:/GSDteams` directory IS the extension repo root. The `.planning/` directory already exists and is for GSD planning docs, not extension source. Do not create anything inside `.planning/` in this task.
  </action>
  <verify>
Run: `ls agents/ workflows/ commands/gsd/ scripts/`
All four directories exist. Each contains a .gitkeep file. No other files present.
  </verify>
  <done>Four directories exist at repo root with .gitkeep placeholders. Directory layout matches PROJECT.md architecture section under "Extension File Layout".</done>
</task>

<task type="auto">
  <name>Task 2: Write templates/TEAM.md starter template with 3 example roles</name>
  <files>
    templates/TEAM.md
  </files>
  <action>
Create `templates/TEAM.md` with the following exact content. This is the starter template that `install.sh` copies to `.planning/TEAM.md` in the user's GSD project (if no TEAM.md already exists there).

Design decisions (from RESEARCH.md Section 4):
- Roles are fully visible (not HTML-commented out) — easier to understand the format
- Each role has a `> Example role — customize before use` blockquote callout so users know to edit them
- YAML code blocks inside each role section are the machine-readable anchor for Phase 2 parsing
- Three roles: Security Auditor, Rules Lawyer, Performance Analyst (in that order)

Write the file with this exact content:

```
---
version: 1
roles:
  - security-auditor
  - rules-lawyer
  - performance-analyst
---

# Review Team

Define your reviewer roles below. Each role runs in isolation — it sees only the sanitized
execution artifact, never the plan, executor reasoning, or prior summaries.

Run `/gsd:new-reviewer` to add a role through a guided conversation, or customize the
example roles below directly.

---

## Role: Security Auditor

> Example role — customize before use. Remove this callout when your role is ready.

```yaml
name: security-auditor
focus: Security vulnerabilities and unsafe patterns
```

**What this role reviews:**
- Authentication and authorization logic
- Input validation and sanitization
- Secrets and credentials handling
- SQL injection, XSS, and injection patterns
- Dependency vulnerabilities introduced by stack changes

**Severity thresholds:**
- `critical`: Hardcoded secrets, SQL injection, missing auth on sensitive routes
- `major`: Missing input validation, insecure defaults, weak session handling
- `minor`: Missing security headers, verbose error messages, deprecated methods

**Routing hints:**
- Critical findings: `block_and_escalate`
- Major findings: `send_for_rework`
- Minor findings: `log_and_continue`

---

## Role: Rules Lawyer

> Example role — customize before use. Remove this callout when your role is ready.

```yaml
name: rules-lawyer
focus: Consistency with stated requirements and project conventions
```

**What this role reviews:**
- Do implemented behaviors match the plan's stated requirements?
- Are coding conventions from CONVENTIONS.md followed?
- Are error handling patterns consistent with existing patterns?
- Are API contracts honored as specified?

**Severity thresholds:**
- `critical`: Implementation directly contradicts a stated requirement
- `major`: Missing required behavior, wrong API contract
- `minor`: Style deviation, naming inconsistency, missing doc comment

**Routing hints:**
- Critical findings: `block_and_escalate`
- Major findings: `send_for_rework`
- Minor findings: `log_and_continue`

---

## Role: Performance Analyst

> Example role — customize before use. Remove this callout when your role is ready.

```yaml
name: performance-analyst
focus: Performance implications of new code and architectural decisions
```

**What this role reviews:**
- N+1 query patterns in new database interactions
- Unbounded loops or O(n²) operations on large datasets
- Missing pagination on list endpoints
- Synchronous blocking calls where async is appropriate
- Memory allocation patterns (large in-memory collections)

**Severity thresholds:**
- `critical`: O(n²) on unbounded production data, blocking main thread
- `major`: Missing pagination, N+1 query pattern, sync where async required
- `minor`: Suboptimal algorithm choice on small/bounded data

**Routing hints:**
- Critical findings: `block_and_escalate`
- Major findings: `send_for_rework`
- Minor findings: `log_and_continue`
```

IMPORTANT: The YAML code blocks inside the role sections use triple-backtick fences with `yaml` language tag. Write these as literal fenced code blocks in the markdown file — not as prose.
  </action>
  <verify>
Run: `grep "## Role:" templates/TEAM.md | wc -l`
Output must be 3.

Run: `grep "name:" templates/TEAM.md`
Must show three lines: `name: security-auditor`, `name: rules-lawyer`, `name: performance-analyst`.

Run: `grep "customize before use" templates/TEAM.md | wc -l`
Must be 3 (one callout per role).
  </verify>
  <done>templates/TEAM.md exists with exactly 3 role sections. Each role has a YAML block with name+focus, a What this role reviews list, severity thresholds, and routing hints. Each role has a visible example callout. File is valid markdown that renders correctly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls agents/ workflows/ commands/gsd/ scripts/` — all four directories exist with .gitkeep files
2. `ls templates/TEAM.md` — file exists
3. `grep -c "## Role:" templates/TEAM.md` — outputs `3`
4. `grep "name:" templates/TEAM.md` — shows all three role names in kebab-case
5. `grep "block_and_escalate\|send_for_rework\|log_and_continue" templates/TEAM.md | wc -l` — output 9 (3 routing lines per role)
</verification>

<success_criteria>
- Extension repo directory scaffold exists with agents/, workflows/, commands/gsd/, scripts/ directories
- templates/TEAM.md exists at repo root, not inside .planning/
- TEAM.md has exactly 3 roles: security-auditor, rules-lawyer, performance-analyst
- Each role has the YAML block, criteria list, severity thresholds, routing hints, and example callout
- File format is parseable by splitting on `## Role:` headers (required by Phase 2 review-team.md workflow)
- Requirements satisfied: TEAM-01 (TEAM.md at .planning/TEAM.md — delivered via install.sh in 01-04), TEAM-04 (starter template with 3 example roles)
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-scaffold-gsd-integration/01-01-SUMMARY.md` following the standard summary template. Key items to document: directory structure created, TEAM.md role format chosen (visible callouts not HTML comments), three role names and their YAML field names (used as anchors by Phase 2 parser).
</output>

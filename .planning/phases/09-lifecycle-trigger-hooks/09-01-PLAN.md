---
phase: 09-lifecycle-trigger-hooks
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - D:/GSDteams/scripts/patch-plan-phase.py
autonomous: true

must_haves:
  truths:
    - "Running patch-plan-phase.py against plan-phase.md inserts a pre_plan_agent_gate section before step 13"
    - "Running patch-plan-phase.py twice on the same file produces an identical result (idempotent)"
    - "The pre-plan gate is always fail-open — any dispatcher error never prevents plan creation"
    - "When agent_studio is disabled the gate produces no output and proceeds silently"
  artifacts:
    - path: "D:/GSDteams/scripts/patch-plan-phase.py"
      provides: "Python patch script that inserts pre_plan_agent_gate into plan-phase.md"
      contains: "pre_plan_agent_gate"
    - path: "D:/GSDteams/scripts/patch-plan-phase.py"
      provides: "Idempotency guard"
      contains: "already patched"
  key_links:
    - from: "D:/GSDteams/scripts/patch-plan-phase.py"
      to: "C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md"
      via: "str.replace(anchor, new_section + anchor, 1)"
      pattern: "pre_plan_agent_gate"
    - from: "pre_plan_agent_gate step in plan-phase.md"
      to: "agent-dispatcher.md"
      via: "@~/.claude/get-shit-done-review-team/workflows/agent-dispatcher.md"
      pattern: "trigger=pre-plan"
---

<objective>
Create scripts/patch-plan-phase.py — the Python patch script that inserts a `pre_plan_agent_gate` step into GSD's plan-phase.md workflow (LIFE-02).

Purpose: When installed, plan-phase.md gains a new step 13 ("Pre-Plan Agent Gate") that fires the agent dispatcher before plan creation. The gate is ALWAYS fail-open — any error, timeout, or missing config silently logs and proceeds to plan creation. This is the wiring step; injecting agent notes into the planner prompt is Phase 10 (ADVY-01).

Output: `D:/GSDteams/scripts/patch-plan-phase.py` — a complete, idempotent Python 3 patch script following the exact pattern of existing Phase 7 patch scripts.
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/GSDteams/.planning/PROJECT.md
@D:/GSDteams/.planning/ROADMAP.md
@D:/GSDteams/.planning/STATE.md

@D:/GSDteams/scripts/patch-execute-plan-dispatcher.py
@D:/GSDteams/.planning/phases/09-lifecycle-trigger-hooks/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/patch-plan-phase.py</name>
  <files>D:/GSDteams/scripts/patch-plan-phase.py</files>
  <action>
Create `D:/GSDteams/scripts/patch-plan-phase.py` following the EXACT pattern of `scripts/patch-execute-plan-dispatcher.py` (read that file first before writing).

The script structure:
- Shebang: `#!/usr/bin/env python3`
- Docstring: describes Phase 9 LIFE-02, usage, idempotency check string
- `import sys, os`
- `def main():` with argv check, file existence check, read file
- Idempotency check: if `'pre_plan_agent_gate'` in content → print SKIP and exit 0
- Anchor check: anchor string must exist in content, else print ERROR and exit 1
- Build new_section string, do str.replace(anchor, new_section + anchor, 1)
- Write patched content back
- Print [OK] with description

**Anchor string** (insert-before pattern — new section goes BEFORE this exact text):
```
## 13. Present Final Status
```
IMPORTANT: Before writing the anchor, READ `C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md` to confirm the exact anchor text character-for-character. If the anchor differs from the above, use what is actually in the file.

**What the patch does:** Renames old step 13 to 14 by inserting the new section as "## 13. Pre-Plan Agent Gate" and simultaneously renaming the anchor from "## 13. Present Final Status" to "## 14. Present Final Status". This requires the anchor to include the old heading and the replacement to include the new heading. The insert pattern is:
- anchor = `\n## 13. Present Final Status\n`
- replacement = `\n## 13. Pre-Plan Agent Gate\n\n{new step body}\n\n## 14. Present Final Status\n`
- patched = content.replace(anchor, replacement, 1)

ALSO check whether "## 14." exists in plan-phase.md (old step 14). If it does, it also needs renaming to "## 15." — add a second str.replace for this. If it doesn't exist, skip the second replace. Read the file first to determine this.

**New step body for ## 13. Pre-Plan Agent Gate:**

```markdown
Check whether pre-plan agents should fire before presenting final status.

**This gate is ALWAYS fail-open.** Any failure here — dispatcher error, agent timeout,
TEAM.md unreadable, config missing — MUST NOT prevent plan creation. Catch all errors
and proceed to step 14 (present final status) regardless.

```bash
AGENT_STUDIO_ENABLED=$(node C:/Users/gutie/.claude/get-shit-done/bin/gsd-tools.cjs config get workflow.agent_studio 2>/dev/null || echo "false")
```

If `AGENT_STUDIO_ENABLED` is not `"true"`: skip this step entirely — no output, no log.
Proceed directly to step 14.

If `AGENT_STUDIO_ENABLED` is `"true"`:

1. Check that `.planning/TEAM.md` exists. If missing: log
   `Pre-plan gate: no TEAM.md found — skipping` and proceed to step 14.

2. If TEAM.md exists: call the dispatcher:
   ```
   @~/.claude/get-shit-done-review-team/workflows/agent-dispatcher.md
   ```
   Pass: trigger=pre-plan, phase_id={phase_dir}, plan_num=pre-plan.

3. If the dispatcher call fails or errors: log
   `Pre-plan gate: dispatcher error — proceeding with plan creation (fail-open)`
   and continue to step 14. Do NOT stop or report failure.

4. Dispatcher output (agent notes, if any) is collected and displayed inline.
   Injection into the planner Task() prompt is Phase 10 (ADVY-01) — not implemented here.
```

**Do NOT** attempt to inject notes into the planner Task() call in step 8 of plan-phase.md. That is Phase 10 scope (ADVY-01). Phase 9 only adds the gate firing and output display.

The complete script follows the exact structure of `patch-execute-plan-dispatcher.py`:
- Same import pattern
- Same file read/write pattern
- Same error message format (f-string with `file=sys.stderr`)
- Same print format for [SKIP] and [OK] results
- No new dependencies (pure Python stdlib)
  </action>
  <verify>
Run against the installed plan-phase.md:
```bash
python3 D:/GSDteams/scripts/patch-plan-phase.py C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md
```
Expected: prints `[OK] Patched: ...` with description of what was inserted.

Then verify idempotency:
```bash
python3 D:/GSDteams/scripts/patch-plan-phase.py C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md
```
Expected: prints `[SKIP] ... already patched`.

Then confirm the patch landed in the file:
```bash
grep -n "pre_plan_agent_gate\|## 13\.\|## 14\." C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md | head -20
```
Expected: "## 13. Pre-Plan Agent Gate" appears before "## 14. Present Final Status".

Confirm NO duplicate step 13:
```bash
grep -c "## 13\." C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md
```
Expected: exactly 1.
  </verify>
  <done>
- `D:/GSDteams/scripts/patch-plan-phase.py` exists and is syntactically valid Python 3
- Running the script against plan-phase.md exits 0 and prints [OK]
- Running it a second time exits 0 and prints [SKIP]
- plan-phase.md contains exactly one "## 13. Pre-Plan Agent Gate" section
- plan-phase.md contains "## 14. Present Final Status" (not ## 13)
- The new section body contains "pre_plan_agent_gate", "fail-open", and the agent-dispatcher.md @-reference
- No "## 13. Present Final Status" heading remains in plan-phase.md (it became ## 14)
  </done>
</task>

</tasks>

<verification>
1. `python3 D:/GSDteams/scripts/patch-plan-phase.py --help` or running without args prints usage to stderr
2. `grep "pre_plan_agent_gate" C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md` returns a match
3. `grep "## 13\." C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md` returns exactly "## 13. Pre-Plan Agent Gate"
4. `grep "## 14\." C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md` returns "## 14. Present Final Status"
5. `grep "fail-open\|ALWAYS fail-open" C:/Users/gutie/.claude/get-shit-done/workflows/plan-phase.md` confirms the safety contract is present
</verification>

<success_criteria>
LIFE-02 patch script deliverable complete:
- scripts/patch-plan-phase.py exists, is runnable, idempotent, and produces the correct output
- plan-phase.md gains "## 13. Pre-Plan Agent Gate" before "## 14. Present Final Status" after running the script
- The pre-plan gate step body calls agent-dispatcher.md with trigger=pre-plan and is explicitly fail-open
- No step numbering conflicts remain in plan-phase.md after patching
</success_criteria>

<output>
After completion, create `D:/GSDteams/.planning/phases/09-lifecycle-trigger-hooks/09-01-SUMMARY.md`
</output>

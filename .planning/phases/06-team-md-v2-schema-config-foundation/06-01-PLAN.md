---
phase: 06-team-md-v2-schema-config-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/TEAM.md
  - .planning/research/ARCHITECTURE.md
autonomous: true

must_haves:
  truths:
    - "templates/TEAM.md has version: 2 integer sentinel (not a string, not version: 1)"
    - "A v1 TEAM.md role (no new fields) is described by the parser spec as producing defaults: mode=advisory, trigger=post-plan, output_type=findings, commit=false, tools=[]"
    - "A v2 role block in templates/TEAM.md with mode: autonomous includes scope: with allowed_paths: glob list and tools: tool name list — both fields present and annotated"
    - "The normalizeRole algorithm is written down in ARCHITECTURE.md (or a new parser-spec section) as the authoritative reference for Phase 7 dispatcher"
    - "templates/TEAM.md shows at least one complete v2 role block alongside the three existing v1 roles"
  artifacts:
    - path: "templates/TEAM.md"
      provides: "v2 TEAM.md starter template"
      contains: "version: 2"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Parser specification with normalizeRole algorithm and defaults table"
      contains: "normalizeRole"
  key_links:
    - from: "templates/TEAM.md"
      to: "ARCHITECTURE.md normalizeRole"
      via: "version sentinel integer drives shim behavior"
      pattern: "version: 2"
    - from: "templates/TEAM.md scope: block"
      to: "success criterion 2"
      via: "allowed_paths glob list + tools tool list both present"
      pattern: "allowed_paths:"
---

<objective>
Update templates/TEAM.md to the v2 schema and write the authoritative parser specification.

Purpose: Every downstream Agent Studio phase (dispatcher, team roster, lifecycle hooks) depends on a single authoritative definition of v2 fields and their defaults. This plan creates that foundation. templates/TEAM.md becomes the reference implementation; ARCHITECTURE.md gains the normalizeRole algorithm as a contractual reference for Phase 7.

Output: Updated templates/TEAM.md (v2, fully annotated), parser spec section added to ARCHITECTURE.md.
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-team-md-v2-schema-config-foundation/06-RESEARCH.md
@templates/TEAM.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update templates/TEAM.md to v2 schema</name>
  <files>templates/TEAM.md</files>
  <action>
    Rewrite templates/TEAM.md in place. Read the current file first.

    CHANGES REQUIRED:

    1. Frontmatter — change version sentinel from 1 to 2 (integer, no quotes):
       ```yaml
       ---
       version: 2
       roles:
         - security-auditor
         - rules-lawyer
         - performance-analyst
         - doc-writer
       ---
       ```
       The `agents:` list is NOT required (per research open question 2 — informational only, dispatcher uses `roles:`). Omit it from the template.

    2. Keep the three existing v1 starter roles (Security Auditor, Rules Lawyer, Performance Analyst) UNCHANGED in their role body. Their YAML config blocks retain only `name:` and `focus:` — no new fields. This demonstrates that v1 role blocks are valid in a v2 TEAM.md (version shim applies defaults at parse time).

    3. Add a new fourth role block: "Doc Writer" — a complete v2 role demonstrating all optional fields including autonomous mode and scope constraints. Place it after the Performance Analyst role, separated by `---`.

       The v2 role block format (Doc Writer example):

       ```
       ## Role: Doc Writer

       ```yaml
       name: doc-writer
       focus: Keeps documentation in sync with implementation

       # V2 fields — all optional. When absent, defaults apply (see parser spec).
       mode: autonomous           # advisory | autonomous  (default: advisory)
       trigger: post-phase        # pre-plan | post-plan | post-phase | on-demand  (default: post-plan)
       output_type: artifact      # findings | artifact | advisory  (default: findings)
       commit: true               # true | false  (default: false)
       commit_message: "docs(auto): update docs after phase {phase}"

       # scope — autonomous agents only. Constrains filesystem and tool access.
       scope:
         allowed_paths:           # glob patterns the agent may read/write
           - "docs/**"
           - "*.md"
           - ".planning/**/*.md"
         allowed_tools:           # Claude Code tool names permitted
           - Read
           - Write
           - Grep
       ```

       **What this role reviews:**
       - README.md accuracy against current implementation
       - API reference docs matching exported contracts
       - Inline code comments and docstrings

       **Severity thresholds:**
       - `critical`: N/A — doc-writer is autonomous, does not produce findings
       - `major`: N/A
       - `minor`: N/A

       **Routing hints:** N/A — output_type: artifact, not findings
       ```

    CRITICAL DETAILS:
    - version: 2 is an integer (no quotes). Do NOT write version: "2".
    - The `allowed_tools:` field name (not `tools:`) is used inside `scope:` per the planner decision: `tools:` at the top-level role block names tool-call access; `allowed_paths:` is a new scope field. Use `allowed_tools:` inside `scope:` to distinguish from a hypothetical top-level `tools:` field — this matches the success criterion which explicitly names `allowed_paths` and `allowed_tools`.
    - The three existing v1 roles must NOT have scope: or mode: fields added. They demonstrate v1 compatibility.
    - Preserve the existing preamble prose and `Run /gsd:new-reviewer` instruction.
    - Add a brief comment above the Doc Writer role explaining it is a v2 example: `<!-- v2 example role: demonstrates autonomous mode, scope constraints, and lifecycle trigger -->`
  </action>
  <verify>
    Read templates/TEAM.md and confirm:
    1. First line of frontmatter shows `version: 2` (not `version: 1`, not `version: "2"`)
    2. Four roles listed in frontmatter `roles:` list
    3. Security Auditor, Rules Lawyer, Performance Analyst yaml blocks contain ONLY `name:` and `focus:` — no mode/trigger/tools fields
    4. Doc Writer yaml block contains: name, focus, mode, trigger, output_type, commit, commit_message, scope (with allowed_paths and allowed_tools)
    5. `allowed_paths:` and `allowed_tools:` both appear inside `scope:` in the Doc Writer block
  </verify>
  <done>
    templates/TEAM.md has version: 2 sentinel, four roles, three v1 role blocks unchanged, one complete v2 Doc Writer role block with scope.allowed_paths and scope.allowed_tools fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add parser specification to ARCHITECTURE.md</name>
  <files>.planning/research/ARCHITECTURE.md</files>
  <action>
    Read .planning/research/ARCHITECTURE.md. Append a new section titled "## Parser Specification (Phase 6 deliverable)" at the end of the document.

    The section must contain:

    1. **Version detection rule** (exact prose):
       "Read the integer `version` field from TEAM.md frontmatter. If `version >= 2`, apply full v2 parsing. If `version == 1` or the field is absent, activate the v1 compatibility shim and inject defaults for all missing v2 fields. Comparison is numeric — `version: 2` (integer) not `version: "2"` (string)."

    2. **Defaults table** for v1 compatibility shim:

       | Field | Default when absent | Notes |
       |-------|---------------------|-------|
       | `mode` | `advisory` | All v1 roles are advisory |
       | `trigger` | `post-plan` | v1 roles fire after each plan, matching existing review pipeline behavior |
       | `tools` | `[]` | No filesystem tool access override |
       | `output_type` | `findings` | v1 roles produce findings, routed through synthesizer |
       | `commit` | `false` | v1 roles do not commit autonomously |
       | `commit_message` | `null` | Not applicable when commit: false |
       | `scope.allowed_paths` | `[]` | No path restrictions declared |
       | `scope.allowed_tools` | `[]` | No tool restrictions declared |

    3. **normalizeRole algorithm** (pseudocode, must be present verbatim):

       ```
       function normalizeRole(role, version):
         if version < 2:
           role.mode             = role.mode             ?? "advisory"
           role.trigger          = role.trigger          ?? "post-plan"
           role.tools            = role.tools            ?? []
           role.output_type      = role.output_type      ?? "findings"
           role.commit           = role.commit           ?? false
           role.commit_message   = role.commit_message   ?? null
           role.scope            = role.scope            ?? {}
           role.scope.allowed_paths  = role.scope.allowed_paths  ?? []
           role.scope.allowed_tools  = role.scope.allowed_tools  ?? []
         return role
       ```

    4. **Key behavioral guarantee** (exact prose):
       "A TEAM.md with `version: 1` (or no version field) loaded through the v2 dispatcher produces identical routing behavior to the Phase 4 review pipeline. advisory mode + post-plan trigger + findings output_type is the v1 behavior path. No user migration is required."

    5. **Field name disambiguation** (one paragraph):
       "The `tools:` field at the top level of a role block is reserved for future use (tool-call access whitelist for the role agent itself). The `scope.allowed_tools:` field inside `scope:` constrains which Claude Code tools an autonomous agent may invoke during its execution. These are distinct concerns. The `scope.allowed_paths:` field specifies glob patterns the autonomous agent may read or write. Both scope fields are relevant only when `mode: autonomous`."

    Do NOT modify any existing content in ARCHITECTURE.md — append only.
  </action>
  <verify>
    Read .planning/research/ARCHITECTURE.md and confirm:
    1. Section "## Parser Specification (Phase 6 deliverable)" exists at the end of the file
    2. `normalizeRole` function pseudocode is present with all 8 field assignments
    3. Defaults table contains all 8 fields with their default values
    4. The phrase "identical routing behavior to the Phase 4 review pipeline" appears (behavioral guarantee)
    5. "scope.allowed_paths" and "scope.allowed_tools" both appear in the spec
  </verify>
  <done>
    ARCHITECTURE.md contains the parser specification section with version detection rule, defaults table, normalizeRole algorithm, behavioral guarantee, and field name disambiguation. This is the authoritative reference for Phase 7 dispatcher implementation.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep "version: 2" D:/GSDteams/templates/TEAM.md` returns a match in the frontmatter
2. `grep "allowed_paths:" D:/GSDteams/templates/TEAM.md` returns a match in the Doc Writer scope block
3. `grep "allowed_tools:" D:/GSDteams/templates/TEAM.md` returns a match in the Doc Writer scope block
4. `grep "normalizeRole" D:/GSDteams/.planning/research/ARCHITECTURE.md` returns a match
5. Read templates/TEAM.md — Security Auditor yaml block must contain ONLY `name:` and `focus:` lines
6. The three v1 starter roles still have all their narrative sections (What this role reviews, Severity thresholds, Routing hints) intact
</verification>

<success_criteria>
- templates/TEAM.md: version: 2 sentinel, four entries in roles:, three v1 role blocks unchanged, one complete v2 Doc Writer block with scope.allowed_paths and scope.allowed_tools
- ARCHITECTURE.md: normalizeRole algorithm + defaults table + version detection rule + behavioral guarantee — Phase 7 implementer has a single authoritative spec to implement against
- No user migration required: the live .planning/TEAM.md (version: 1, three starter roles) continues to work exactly as in v1.0 — no changes to that file in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/06-team-md-v2-schema-config-foundation/06-01-SUMMARY.md` following the summary template.
</output>

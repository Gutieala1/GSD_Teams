---
phase: 03-single-reviewer-finding-schema
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - workflows/review-team.md
autonomous: true

must_haves:
  truths:
    - "The spawn_reviewers step in review-team.md is implemented (placeholder replaced) for a single reviewer"
    - "spawn_reviewers selects the first valid role from validate_team's roles list"
    - "Role definition text is extracted from TEAM.md by finding the ## Role: section and reading to the next ## Role: or EOF"
    - "Task() is spawned with subagent_type='gsd-reviewer' and role_definition injected via <role_definition> tags"
    - "JSON return is parsed and findings array is extracted"
    - "Empty findings array is handled gracefully (log '0 findings', not an error)"
    - "return_status step is updated to reflect Phase 3 endpoint (not Phase 2)"
    - "The sanitize step's ARTIFACT_PATH variable is reused in spawn_reviewers (not recomputed)"
  artifacts:
    - path: "workflows/review-team.md"
      provides: "Updated review-team.md with spawn_reviewers implemented for single reviewer"
      contains: "spawn_reviewers"
  key_links:
    - from: "workflows/review-team.md spawn_reviewers"
      to: "agents/gsd-reviewer.md"
      via: "Task(subagent_type='gsd-reviewer') with role_definition injection"
      pattern: "gsd-reviewer"
    - from: "spawn_reviewers"
      to: "ARTIFACT_PATH"
      via: "reuse of ARTIFACT_PATH computed in sanitize step"
      pattern: "ARTIFACT_PATH"
    - from: "spawn_reviewers"
      to: "validate_team roles list"
      via: "first_role_name = roles_list[0]"
      pattern: "roles_list\\[0\\]"
---

<objective>
Update `workflows/review-team.md` to implement the spawn_reviewers step for a single reviewer.

Purpose: Phase 3's first end-to-end reviewer firing. The spawn_reviewers placeholder (written in Phase 2) is replaced with a concrete implementation that: (1) selects the first valid role from TEAM.md, (2) extracts the full role section text, (3) spawns gsd-reviewer.md via Task() with role injection, (4) parses the JSON return, and (5) logs findings. Phase 3 stops here — Phase 4 adds synthesis and routing.

Output: Modified workflows/review-team.md — the spawn_reviewers step is implemented, return_status is updated for Phase 3.

Note: This plan EDITS an existing file. Use the Edit tool, not Write. Preserve all existing content (validate_team, sanitize steps) exactly as-is. Replace only the spawn_reviewers step content and the return_status step content.
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-single-reviewer-finding-schema/03-RESEARCH.md
@workflows/review-team.md
@agents/gsd-reviewer.md
@.planning/phases/03-single-reviewer-finding-schema/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement spawn_reviewers step in review-team.md</name>
  <files>workflows/review-team.md</files>
  <action>
Read the current `workflows/review-team.md` first. Then edit the file to replace the spawn_reviewers step placeholder and update the return_status step. Preserve ALL other content (purpose block, inputs block, validate_team step, sanitize step) exactly as-is.

**Replace the spawn_reviewers step content:**

Replace the current placeholder content inside `<step name="spawn_reviewers">`:
```
## Step 3: Spawn Reviewer Agents

[Phase 3 -- not yet implemented]

This step will:
1. Read each valid role from TEAM.md (using role names from validate_team step)
2. Spawn reviewer agents in parallel via Task() -- one per role
3. Each reviewer receives: ARTIFACT.md path + their role definition
4. Collect all reviewer returns (structured findings)

See ROADMAP.md Phase 3 for design.
```

With this implementation:

```
## Step 3: Spawn Reviewer (Phase 3 — Single Reviewer)

Phase 3 implements this step for exactly ONE reviewer — the first valid role from TEAM.md.
Phase 4 will expand to parallel spawning of all roles.

### Select First Valid Role

Use the valid roles list from the validate_team step. Take the first role:
```
first_role_name = roles_list[0]
```

### Extract Role Definition Text

Read `.planning/TEAM.md` using the Read tool. Extract the full text of the section for `first_role_name`:

1. Find the line matching `## Role: {first_role_name}` (case-insensitive match on the name from the YAML `name:` field)
2. Extract ALL text from that `## Role:` header line to the NEXT `## Role:` header line (exclusive), or to end-of-file if no next role exists
3. This entire extracted text is the `role_definition_text` to inject

Example: For role name `security-auditor`, find `## Role: Security Auditor` (or equivalent), extract everything until `## Role: Rules Lawyer` begins.

The `role_definition_text` must include the full role section: YAML code block, focus description, review checklist, severity thresholds, and routing hints — everything written for that role.

### Spawn Reviewer

```
Task(
  subagent_type="gsd-reviewer",
  prompt="
    <objective>
    Review the sanitized artifact against your assigned role criteria.
    Return structured findings as JSON.
    </objective>

    <inputs>
    artifact_path: ${ARTIFACT_PATH}
    phase: ${PHASE_ID}
    plan: ${PLAN_NUM}
    </inputs>

    <role_definition>
    ${role_definition_text}
    </role_definition>

    <execution_context>
    @~/.claude/get-shit-done-review-team/agents/gsd-reviewer.md
    </execution_context>
  "
)
```

Note: `${ARTIFACT_PATH}` is the value computed in the sanitize step. Do not recompute it — reuse the same path.

### Collect and Log JSON Return

After the reviewer returns:

1. Parse the reviewer's return as JSON to extract the `findings` array
2. Log: `Review Team: Reviewer {first_role_name}: {N} finding(s)`
3. If the findings array is empty: log `Review Team: Reviewer {first_role_name}: 0 findings (no issues in domain)` and continue — this is valid, not an error
4. If findings are present: log each finding's id, severity, and description for audit:
   `  [{id}] [{severity}] {description}`
5. Store the full findings JSON for the return_status block

Phase 3 stops here — Phase 4 adds synthesis and deterministic routing.
```

**Replace the return_status step content:**

Replace the entire content inside `<step name="return_status">`:

Current content (replace this):
```
## Step 5: Return Pipeline Status

After the sanitize step completes (Phase 2 endpoint), return:

```markdown
## REVIEW PIPELINE: SANITIZE COMPLETE

**Phase:** ${PHASE_ID}
**Plan:** ${PLAN_NUM}
**Roles found:** {N} ({role names})
**Artifact:** ${ARTIFACT_PATH}

[Phase 3/4: Reviewer spawning and synthesis not yet implemented]
[Pipeline stops here in Phase 2 -- review-team.md will be extended in later phases]
```

When Phase 3 and Phase 4 are implemented, this return format will be replaced with the full
synthesis result including routing decision and REVIEW-REPORT.md path.
```

With this Phase 3 implementation:

```
## Step 5: Return Pipeline Status

After the spawn_reviewers step completes (Phase 3 endpoint), return:

```markdown
## REVIEW PIPELINE: REVIEWER COMPLETE

**Phase:** ${PHASE_ID}
**Plan:** ${PLAN_NUM}
**Roles found:** {N} ({comma-separated role names})
**Artifact:** ${ARTIFACT_PATH}
**Reviewer fired:** {first_role_name}
**Findings:** {finding count} finding(s)

{If findings exist, list each:}
| ID | Severity | Description |
|----|----------|-------------|
| {id} | {severity} | {description} |

{If 0 findings:}
No findings in {first_role_name} domain for this plan.

[Phase 4: Parallel multi-reviewer spawning, synthesis, and routing not yet implemented]
[Pipeline stops here in Phase 3 -- review-team.md will be extended in Phase 4]
```
```

The `<purpose>` block at the top of the file should also be updated. Change:
```
**Phase 2 implements:** TEAM.md validation and sanitizer spawning (Steps 1 and 2).
**Phase 3 adds:** Reviewer agent spawning in parallel (Step 3).
**Phase 4 adds:** Synthesis and routing (Step 4).
```
To:
```
**Phase 2 implemented:** TEAM.md validation and sanitizer spawning (Steps 1 and 2).
**Phase 3 implements:** Single reviewer spawning and JSON findings collection (Step 3).
**Phase 4 adds:** Parallel reviewer spawning, synthesis, and routing (Steps 3 expanded + Step 4).
```
  </action>
  <verify>Read workflows/review-team.md and confirm: (1) spawn_reviewers step no longer contains '[Phase 3 -- not yet implemented]'; (2) spawn_reviewers contains 'first_role_name = roles_list[0]'; (3) Task() call includes subagent_type="gsd-reviewer" and &lt;role_definition&gt; tags; (4) ARTIFACT_PATH is referenced from the sanitize step (not recomputed); (5) empty findings case is handled with 'log and continue'; (6) return_status says 'REVIEWER COMPLETE' not 'SANITIZE COMPLETE'; (7) validate_team and sanitize steps are unchanged.</verify>
  <done>workflows/review-team.md spawn_reviewers step is fully implemented for Phase 3 (single reviewer). The step: selects first valid role, extracts full TEAM.md role section text, spawns gsd-reviewer via Task() with role injection, parses JSON return, logs findings (including empty array as valid output), and returns a REVIEWER COMPLETE status block. The validate_team and sanitize steps remain exactly as written in Phase 2.</done>
</task>

</tasks>

<verification>
Read workflows/review-team.md and verify the full pipeline flow:
1. validate_team step — unchanged from Phase 2 (three-condition validation, roles list produced)
2. sanitize step — unchanged from Phase 2 (ARTIFACT_PATH computed, sanitizer spawned)
3. spawn_reviewers step — Phase 3 implementation present:
   - roles_list[0] selected as first_role_name
   - TEAM.md role section extracted (## Role: header to next ## Role: or EOF)
   - Task() with subagent_type="gsd-reviewer" and &lt;role_definition&gt; injection
   - ARTIFACT_PATH reused from sanitize step
   - Empty findings array handled as valid (not error)
   - Findings logged per-finding with id, severity, description
4. return_status step — updated to Phase 3 endpoint (REVIEWER COMPLETE format)
5. synthesize step — unchanged Phase 4 placeholder
</verification>

<success_criteria>
workflows/review-team.md spawn_reviewers step is implemented and satisfies REVR-01 and REVR-06:
- REVR-01: Reviewer receives only ARTIFACT.md path and its own role definition (no SUMMARY.md, no other reviewer results)
- REVR-06: Reviewer spawned without modification — role definition injected at spawn time via &lt;role_definition&gt; tags
- Empty findings handled gracefully
- Phase 4 placeholder (synthesize step) preserved intact
- Full pipeline status returned after reviewer completes
</success_criteria>

<output>
After completion, create `.planning/phases/03-single-reviewer-finding-schema/03-03-SUMMARY.md` using the standard SUMMARY.md template.
</output>

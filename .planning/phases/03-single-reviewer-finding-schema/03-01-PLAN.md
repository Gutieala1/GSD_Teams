---
phase: 03-single-reviewer-finding-schema
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/finding-schema.md
autonomous: true

must_haves:
  truths:
    - "schemas/finding-schema.md exists as a standalone reference document"
    - "All 7 required fields are defined: id, reviewer, domain, severity, evidence, description, suggested_routing"
    - "Severity rubric defines all 5 levels (info, low, medium, high, critical) with concrete examples"
    - "Tie-breaking rule ('when in doubt, go lower') is explicitly stated"
    - "Empty findings case ({\"findings\": []}) is documented as a valid expected output"
    - "ID generation rule (role-prefix + zero-padded sequence) is documented"
  artifacts:
    - path: "schemas/finding-schema.md"
      provides: "Finding JSON schema + severity rubric — shared reference for gsd-reviewer.md and future gsd-review-synthesizer.md"
      contains: "finding-schema"
  key_links:
    - from: "schemas/finding-schema.md"
      to: "agents/gsd-reviewer.md"
      via: "embedded in Plan 03-02 — schema fields and severity rubric copied into agent"
      pattern: "id.*reviewer.*domain.*severity.*evidence.*description.*suggested_routing"
---

<objective>
Define the Finding JSON schema and severity rubric as a standalone schema document.

Purpose: Establishes the contract that gsd-reviewer.md (03-02) produces and gsd-review-synthesizer.md (Phase 4) consumes. Every finding that flows through the review pipeline must conform to this schema. The schema document is also the reference for the severity rubric embedded in the reviewer agent.

Output: schemas/finding-schema.md — a single self-contained schema reference document.
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-single-reviewer-finding-schema/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schemas/finding-schema.md</name>
  <files>schemas/finding-schema.md</files>
  <action>
Create `schemas/finding-schema.md` at the repo root (alongside `agents/`, `workflows/`, `templates/`). This is a standalone reference document — not a code file, not a GSD agent. It documents the JSON schema that all reviewer agents produce and all synthesizer agents consume.

The document must contain these sections in order:

**Section 1 — Schema Header**
Title: `# Finding JSON Schema`
Brief purpose: "Defines the JSON structure produced by gsd-reviewer.md and consumed by gsd-review-synthesizer.md. Every finding flowing through the review pipeline must conform to this schema."
Version: `v1.0 (Phase 3)`

**Section 2 — Full Schema Example**
A complete fenced JSON code block showing a finding object inside a `findings` array, with all 7 fields populated with realistic example values:

```json
{
  "findings": [
    {
      "id": "SEC-001",
      "reviewer": "security-auditor",
      "domain": "security",
      "severity": "high",
      "evidence": "JWT_SECRET | required env var | .env",
      "description": "JWT_SECRET stored in .env with no indication of secret rotation policy or minimum entropy requirement.",
      "suggested_routing": "send_for_rework"
    }
  ]
}
```

**Section 3 — Field Reference**
A markdown table with columns: `Field | Type | Required | Constraints | Notes`

Document all 7 fields:
- `id`: string, YES, Format `{ROLE_PREFIX}-{NNN}` (zero-padded 3 digits), Role prefix = first 3-4 uppercase characters of role name (e.g., SEC for security-auditor, LAW for rules-lawyer, PERF for performance-analyst). Sequential within a reviewer's response.
- `reviewer`: string, YES, Must match role `name:` field from TEAM.md YAML exactly, Machine-readable role identifier.
- `domain`: string, YES, Must match role `focus:` field from TEAM.md, Enables synthesizer to detect scope bleed.
- `severity`: string enum, YES, One of: `critical`, `high`, `medium`, `low`, `info` (exact strings), Case-sensitive.
- `evidence`: string, YES, Character-for-character quote from ARTIFACT.md — not a paraphrase, The literal text that grounds the finding. Paraphrase is not valid.
- `description`: string, YES, Observable issue — no speculation, no "might", "could", "probably", States what IS wrong, not what MIGHT be wrong.
- `suggested_routing`: string enum, YES, One of: `block_and_escalate`, `send_for_rework`, `send_to_debugger`, `log_and_continue`, Reviewer suggestion; Phase 4 synthesizer enforces severity-based minimum.

**Section 4 — Severity Rubric**
Title: `## Severity Rubric`
Opening: "Assign severity based on production impact. Use your role's specific severity thresholds from your role definition to calibrate these general definitions."

Define all 5 levels. Use the exact level strings from REVR-05:

**CRITICAL:** Would cause data loss, security breach, or complete system failure in production. No reasonable person would ship code with this finding unresolved. Production deploy must halt.
Examples: Hardcoded credentials, SQL injection, auth bypass, data corruption.

**HIGH:** Incorrect behavior that would reach users or break core functionality. Fix required before shipping. Does not warrant halting a deploy if a workaround exists.
Examples: Race condition in payment processing, missing authentication on a route.

**MEDIUM:** Deviates from stated requirements or established patterns. Should fix before next plan executes. Acceptable risk for a hotfix deploy.
Examples: Missing input validation on an API field, wrong HTTP status code returned.

**LOW:** Suboptimal but working. Degrades quality without breaking behavior. Fix before the phase completes. Fine to ship with a tracking issue.
Examples: Missing database index on a queried column, deprecated API in use.

**INFO:** Observation worth recording. No action required. For awareness only.
Examples: Unused env var, style deviation with no behavioral impact.

**Section 5 — Tie-Breaking Rule**
Title: `## Tie-Breaking Rule`

State verbatim:
```
When deciding between two severity levels, choose the LOWER one.

Tie-breaking question: "Would I personally halt a production deploy for this finding?"
  YES → critical or high
  NO  → medium, low, or info

Anti-inflation check: Review each `critical` finding before finalizing.
If you would not personally stop a production deploy for it, downgrade to `high`.
```

**Section 6 — Routing Field Reference**
Title: `## Routing Destinations`
A table: `Destination | When to use`

- `block_and_escalate`: Critical finding that must be seen by the user before execution continues. Phase 4 hardcodes this for all `critical` severity findings.
- `send_for_rework`: Finding indicates the plan output is incorrect — the plan should be re-executed with corrections.
- `send_to_debugger`: Finding indicates a runtime error or unexpected behavior needing debugging.
- `log_and_continue`: Finding is worth recording but does not block execution.

**Section 7 — Empty Findings Case**
Title: `## Empty Findings`

State: "When a reviewer has no findings in its domain, it returns:"
```json
{"findings": []}
```
"This is a valid, expected output. An empty findings array means the reviewer found nothing in its domain — not that the reviewer failed. The workflow handles empty arrays by logging '0 findings' and continuing normally."

**Section 8 — ID Generation**
Title: `## ID Generation`

State the rule:
- Format: `{ROLE_PREFIX}-{NNN}` where NNN is zero-padded (001, 002, 003...)
- Prefix derivation: First 3-4 uppercase characters of the role's `name:` field from TEAM.md YAML
- Starter role prefixes: `SEC` (security-auditor), `LAW` (rules-lawyer), `PERF` (performance-analyst)
- IDs are sequential within a single reviewer's response starting from 001
- In Phase 4 (parallel), each reviewer's unique prefix prevents ID collisions across the findings set
  </action>
  <verify>Read schemas/finding-schema.md and confirm: (1) file exists, (2) all 7 fields are in the Field Reference table, (3) all 5 severity levels are present with their exact string values, (4) tie-breaking rule section exists and includes the anti-inflation check, (5) empty findings JSON is shown, (6) all 4 routing destinations are listed.</verify>
  <done>schemas/finding-schema.md exists with all 8 sections populated. The 7-field schema, 5-level severity rubric with tie-breaking rule, and 4 routing destinations are all documented. The document is readable as a standalone reference without any other files.</done>
</task>

</tasks>

<verification>
Read schemas/finding-schema.md and verify:
- All 7 required fields documented: id, reviewer, domain, severity, evidence, description, suggested_routing
- Severity enum values exactly match REVR-05: critical, high, medium, low, info (in rubric and field definition)
- Tie-breaking rule present with anti-inflation check
- Empty findings case shown as {"findings": []}
- All 4 routing destinations listed: block_and_escalate, send_for_rework, send_to_debugger, log_and_continue
- ID generation rule explains prefix derivation and zero-padded sequence
</verification>

<success_criteria>
schemas/finding-schema.md exists and is a standalone reference document covering:
1. Complete 7-field JSON schema with example
2. Field-by-field constraints (types, required, valid values)
3. 5-level severity rubric with concrete examples per level
4. Tie-breaking rule with anti-inflation check
5. All 4 routing destinations defined
6. Empty findings case documented as valid output
7. ID generation rule with prefix derivation examples
</success_criteria>

<output>
After completion, create `.planning/phases/03-single-reviewer-finding-schema/03-01-SUMMARY.md` using the standard SUMMARY.md template.
</output>

---
phase: 05-new-reviewer-starter-roles-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/TEAM.md
  - .planning/TEAM.md
autonomous: true

must_haves:
  truths:
    - "A user can run the extension without editing TEAM.md and get useful findings from all 3 roles"
    - "No role contains the 'Example role — customize before use.' callout"
    - "Rules Lawyer criteria do not reference CONVENTIONS.md (a project-specific artifact)"
    - "All 3 roles pass validate_team logic: ## Role: header + YAML name: field + at least one review criterion"
    - "templates/TEAM.md and .planning/TEAM.md are identical"
  artifacts:
    - path: "templates/TEAM.md"
      provides: "Starter role template distributed to users via install.sh"
      contains: "## Role: Security Auditor"
    - path: ".planning/TEAM.md"
      provides: "Active role definitions for the review pipeline"
      contains: "## Role: Rules Lawyer"
  key_links:
    - from: "templates/TEAM.md"
      to: ".planning/TEAM.md"
      via: "install.sh cp command (Section 8)"
      pattern: "Must be identical — install.sh copies template to project"
    - from: ".planning/TEAM.md"
      to: "workflows/review-team.md validate_team step"
      via: "Read tool in validate_team step"
      pattern: "## Role:.*\\nyaml.*name:.*What this role reviews:"
---

<objective>
Upgrade the three starter roles in TEAM.md from "example" status to production-ready.

Purpose: Users who run `bash install.sh` should get a working review team immediately — no editing required. The current files have an `> Example role — customize before use.` callout in every role and one Rules Lawyer criterion that references a project-specific file (CONVENTIONS.md).

Output: Updated `templates/TEAM.md` and `.planning/TEAM.md` with identical content — callouts removed, Rules Lawyer criterion generalized, all three roles immediately usable.
</objective>

<execution_context>
@C:/Users/gutie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gutie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TEAM.md
@templates/TEAM.md
@workflows/review-team.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove example callouts and fix Rules Lawyer criterion in both TEAM.md copies</name>
  <files>templates/TEAM.md, .planning/TEAM.md</files>
  <action>
Read both files. They are currently identical. Make the following changes to BOTH files:

**Change 1 — Remove example callout from Security Auditor.**
Delete this line (including the blank line after it):
```
> Example role — customize before use. Remove this callout when your role is ready.
```
The Security Auditor role content is already production-quality. Only the callout needs removal.

**Change 2 — Remove example callout from Rules Lawyer.**
Same deletion as above.

**Change 3 — Fix the Rules Lawyer convention criterion.**
Replace:
```
- Are coding conventions from CONVENTIONS.md followed?
```
With:
```
- Are coding conventions followed consistently with the existing codebase?
```
Reason: "CONVENTIONS.md" is project-specific. The generalized phrasing makes the role applicable to any GSD project.

**Change 4 — Remove example callout from Performance Analyst.**
Same deletion as above.

**Change 5 — Update the preamble paragraph.**
The current preamble says:
```
Run `/gsd:new-reviewer` to add a role through a guided conversation, or customize the
example roles below directly.
```
Change "example roles" to "starter roles":
```
Run `/gsd:new-reviewer` to add a role through a guided conversation, or customize the
starter roles below directly.
```

After changes, both files must be written with identical content. Write templates/TEAM.md first, then write .planning/TEAM.md with exactly the same content.

The final structure for each role section must be:
```
---

## Role: [Name]

```yaml
name: [slug]
focus: [focus line]
```

**What this role reviews:**
- [item]
...

**Severity thresholds:**
- `critical`: [example]
- `major`: [example]
- `minor`: [example]

**Routing hints:**
- Critical findings: `block_and_escalate`
- Major findings: `send_for_rework`
- Minor findings: `log_and_continue`

---
```

No blockquote callout appears anywhere in the file.
  </action>
  <verify>
Run the following checks:

1. Confirm no callout remains:
```bash
grep -n "Example role" D:/GSDteams/templates/TEAM.md D:/GSDteams/.planning/TEAM.md
```
Expected: no output (grep returns nothing).

2. Confirm CONVENTIONS.md reference is gone:
```bash
grep -n "CONVENTIONS.md" D:/GSDteams/templates/TEAM.md D:/GSDteams/.planning/TEAM.md
```
Expected: no output.

3. Confirm all three role headers are present in both files:
```bash
grep -n "^## Role:" D:/GSDteams/templates/TEAM.md D:/GSDteams/.planning/TEAM.md
```
Expected: 6 lines total (3 per file): Security Auditor, Rules Lawyer, Performance Analyst.

4. Confirm all three YAML name fields are present in both files:
```bash
grep -n "^name:" D:/GSDteams/templates/TEAM.md D:/GSDteams/.planning/TEAM.md
```
Expected: 6 lines total (3 per file): security-auditor, rules-lawyer, performance-analyst.

5. Confirm files are identical:
```bash
diff D:/GSDteams/templates/TEAM.md D:/GSDteams/.planning/TEAM.md
```
Expected: no output (files are identical).
  </verify>
  <done>
Both templates/TEAM.md and .planning/TEAM.md are identical, contain no "Example role" callout, contain no "CONVENTIONS.md" reference, and all three roles have valid ## Role: headers, YAML name: fields, and review criteria lists.
  </done>
</task>

</tasks>

<verification>
1. `grep "Example role" templates/TEAM.md .planning/TEAM.md` returns nothing
2. `grep "CONVENTIONS.md" templates/TEAM.md .planning/TEAM.md` returns nothing
3. `grep "^## Role:" templates/TEAM.md` returns exactly 3 matches
4. `grep "^## Role:" .planning/TEAM.md` returns exactly 3 matches
5. `diff templates/TEAM.md .planning/TEAM.md` returns nothing (files identical)
6. Rules Lawyer criterion reads "Are coding conventions followed consistently with the existing codebase?"
7. Preamble says "starter roles" not "example roles"
</verification>

<success_criteria>
Three production-ready starter roles exist in both templates/TEAM.md and .planning/TEAM.md. A user who runs `bash install.sh` on a fresh GSD project gets a TEAM.md that drives the review pipeline without any manual editing. All three roles satisfy the validate_team logic in workflows/review-team.md (## Role: header + YAML name: + criteria list).
</success_criteria>

<output>
After completion, create `.planning/phases/05-new-reviewer-starter-roles-docs/05-01-SUMMARY.md`
</output>
